# アートワークデザイン方針

## 1. 体験の方向性

研究室の小さな生産端末から始まり、機械化、AI自律生産、ナノ製造、軌道工業、物質変換へ拡大する過程を、情報密度と光の変化で感じられる画面にする。

ゲームの操作は明快なまま保ち、装飾よりも「いま何が起き、次に何が解放されるか」をリッチに伝えることを優先する。

## 2. 現状評価

### 維持する点

- 金属、端末、AIを連想させる暗色とシアンの配色
- 現在のクリップ数を最上位に置く情報階層
- 細い罫線、等幅文字、状態ランプによる制御端末らしさ
- パネル単位の明確な区分とレスポンシブな1カラム化
- 透明背景のペーパークリップ素材を主操作へ使う構成

### 改善する点

- 現在はすべての進行段階で画面の印象が同じため、規模拡大の実感が弱い
- 次の解放条件が画面から分からず、待つ目的が見えにくい
- 設備カードの生産情報が控えめで、購入後の効果を比較しづらい
- イベント通知が消えると、直前に何が起きたか確認できない
- 設備数が増えた場合に一覧が単調になり、上位設備の特別感が出にくい

## 3. 一巡目のデザイン目標

### 3.1 フェーズ表示

- ヘッダーのシステム状態付近に現在の生産フェーズを表示する
- フェーズ名はシアンの小さなラベルとし、現在地を常時認識できるようにする
- フェーズ移行時は画面全体を妨げない短い発光と通知で知らせる
- 将来はフェーズごとにアクセント色を変えられる構造を想定する

### 3.2 次の目標パネル

- メイン生産パネル下部に横長の目標領域を追加する
- 左側に「次の設備」と設備名、右側に必要累計数を表示する
- 細いプログレスバーに現在の進行率を表示する
- 数値だけでなくパーセントも併記し、桁が大きくても進捗を読み取れるようにする
- すべて解放済みの場合は「次の桁へ」という継続目標に切り替える

### 3.3 まとめ買い操作

- 設備パネル見出しの近くに `×1`、`×10`、`MAX` のセグメントボタンを配置する
- 選択中は塗りつぶし、未選択は輪郭表示にする
- 色だけに頼らず `aria-pressed` と形状差で状態を伝える
- スマートフォンでは3ボタンが横幅いっぱいに均等配置されるようにする

### 3.4 設備カード

- 所有数を右上のバッジとして強調する
- 1台当たり生産量よりも、現在その設備全体が生産している量を主情報にする
- 上位設備ほどアイコン枠に細かな光を加えるが、常時アニメーションは避ける
- 購入可能なカードはボタンだけでなくカードの境界にもわずかなシアンを加える
- 設備が8種類になっても視線移動が過剰にならない密度を保つ

### 3.5 システムログ

- 統計パネルの下に端末出力風のシステムログを配置する
- 各項目に時刻、短い種別コード、内容を表示する
- 最新項目だけアクセント色を持ち、古い項目は徐々に弱い色にする
- ログ領域は固定高さとし、画面の伸縮を防ぐ
- スクリーンリーダーにはトーストと重複して過剰に読み上げさせない

## 4. フェーズ別の視覚言語

| フェーズ | 視覚的な意味 | アクセント候補 |
|---|---|---|
| 手動プロトコル | 小規模、手作業、静かな端末 | スチールグレー |
| 機械化 | 安定した反復、自動化 | シアン |
| AI自律生産 | 計算、観測、自己改善 | 青紫 |
| ナノスケール製造 | 精密、微細、高密度 | エメラルド |
| 軌道工業化 | 広域、宇宙、巨大施設 | アンバー |
| 物質変換 | 現実の書き換え、最終的な不穏さ | マゼンタ寄りの白光 |
| 恒星規模採取 | 恒星そのものを資源として扱う規模感 | 白熱に近い高輝度シアン |
| 因果地平到達 | 観測可能な範囲すべてが最適化対象になった静けさ | 淡いマゼンタ寄りの白光（物質変換より僅かに強く） |

一巡目ではレイアウトと情報表現を優先し、全面的な配色切り替えは将来の素材追加時に行う。二巡目でも同様に全面的な配色切り替えは行わないが、8.3節の範囲に限り最終フェーズの気配を追加する。

## 5. 画像素材の方針

- 現在のペーパークリップ画像は、主操作の象徴として継続使用する
- 一巡目では新しい大判画像を増やさず、設備アイコンを記号とCSSで区別する
- 次回以降、フェーズ背景または上位設備の代表画像を1素材ずつ追加する
- 追加画像は `ARTWORK.md` の規則に従い、元素材、説明、軽量版、素材台帳を揃える
- 情報を伝える設備画像には代替テキストを設定し、記号の置換だけの場合は設備名との重複を避ける

## 6. アクセシビリティ

- 進行率はバーの長さだけでなくテキストでも表示する
- 購入数量は `aria-pressed` で通知する
- フェーズ色は補助表現とし、名称を常に表示する
- システムログは自動読み上げ領域にせず、必要なイベントだけ既存通知で伝える
- フェーズ移行時の発光は `prefers-reduced-motion` で抑制する

## 7. 一巡目の完了条件

1. 現在フェーズがヘッダーまたは主要領域で確認できる
2. 次の設備と進行率がメインパネルに表示される
3. まとめ買いの選択状態が明確に分かる
4. 設備全体の生産量をカード上で比較できる
5. 最新6件のシステムログを確認できる
6. PCとスマートフォンの両方で情報が重ならない
7. Web版と試用版で同じ見た目になる

## 8. 二巡目：設備マイルストーン

### 8.1 表現の目的

設備カードを見るだけで「あと何台で大きく強化されるか」が分かり、価格比較だけでなく所有数を伸ばす動機が生まれるようにする。マイルストーンは補助情報として扱い、購入ボタン、価格、現在生産量の視認性を上回らないようにする。

### 8.2 カード内の配置

- 設備説明と生産量の下に、1行のマイルストーン領域を追加する
- 左に次の段階名、右に `現在数 / 目標数` と現在倍率を表示する
- その下に高さ3px程度の細い進捗線を置く
- 進捗線はメイン目標のバーより弱い明度にして、画面内の主従を保つ
- 所有数100以上では段階名を「完全最適化」、進捗を完了状態にする

### 8.3 視覚状態

| 状態 | 表現 |
|---|---|
| 未達成 | 弱いグレーの進捗線と次の段階名 |
| 進行中 | 所有数に応じてシアンの線が伸びる |
| 到達直後 | カード境界と倍率を短く発光させる |
| 完全達成 | `×16` を小さな塗りつぶしバッジで固定表示 |

- 倍率は `×1`、`×2`、`×4`、`×8`、`×16` の短い表記にする
- 達成状態を色だけに頼らず、段階名と数値で明示する
- 全カードを常時アニメーションさせない

### 8.4 達成フィードバックと統計

- 到達通知は既存トーストとシステムログを使用する
- 一度に複数段階へ到達した場合は、最上位の段階だけをトーストで知らせる
- システムログのコードは `MILE` とする
- 統計パネルに `達成数 / 48` を等幅数字で追加する
- カード発光は `prefers-reduced-motion` で抑制する

### 8.5 二巡目の完了条件

1. 12設備すべてで次の所有数目標が読める
2. 現在倍率と進捗が設備カード内で比較できる
3. 購入価格、生産量、マイルストーンの情報階層が崩れない
4. スマートフォン幅で進捗表示が購入ボタンと重ならない
5. 完全達成状態が色なしでも判別できる
6. 統計から全体の達成数を確認できる

## 9. 三巡目：実績パネル

### 9.1 目的と配置

- 実績は長期目標の一覧として、設備・アップグレードより下の全幅パネルに配置する
- 現在の主要操作を押し下げないよう、初期表示では密度の高い小型カードを使用する
- 見出し右側に `解除数 / 14` と実績による全設備倍率をまとめて表示する
- PCでは3カラム、中間幅では2カラム、小型端末では1カラムにする

### 9.2 実績カード

- 左に正方形のステータスマーク、右に名称と条件を置く
- 解除済みマークはチェック記号、未解除は鍵記号とする
- 解除済みは通常の文字色とシアンの細い境界を使用する
- 未解除は低コントラストにするが、名称と条件は読める状態を保つ
- 実績の条件が秘密ではないため、未解除でも名称と条件を隠さない
- カードの高さを揃え、14件を一覧したときの走査性を優先する

### 9.3 解除フィードバック

- 新規解除時は対象カードを短く発光させる
- トーストとログが主な通知であり、カードの動きは補助に留める
- `prefers-reduced-motion` では発光アニメーションを抑制する
- 同時解除でも画面全体を覆う演出は行わない

### 9.4 情報階層とアクセシビリティ

- 見出しの全体倍率を解除数よりやや強く表示する
- 鍵とチェックだけに頼らず、カードへ「未解除」「解除済み」のテキスト状態を設定する
- 実績一覧自体をライブリージョンにせず、解除通知だけを既存トーストで読み上げる
- ボーナスは `全設備 ×1.00` の形式で表示する

### 9.5 三巡目の完了条件

1. 全14件を一覧できる
2. 解除状態を色、記号、テキストで判別できる
3. 現在の実績倍率と解除数が見出しから分かる
4. PC・タブレット・スマートフォンでカードが重ならない
5. 既存の生産操作と設備一覧が実績パネルより先に現れる

## 8. 二巡目：設備マイルストーン

### 8.1 表現の目的

設備カードを見るだけで「あと何台で大きく強化されるか」が分かり、単なる価格比較だけでなく所有数を伸ばす動機が生まれるようにする。

マイルストーンは設備カードの補助情報として扱い、購入ボタン、価格、現在生産量の視認性を上回らないようにする。

### 8.2 カード内の配置

- 設備の説明と生産量の下に、1行のマイルストーン領域を追加する
- 左に次の段階名、中央から右に `現在数 / 目標数` と現在倍率を表示する
- その下に高さ3px程度の細い進捗線を置く
- 進捗線はメイン目標のバーより弱い明度にして、画面内の主従を保つ
- 所有数100以上では段階名を「完全最適化」、進捗を完了状態にする

### 8.3 視覚状態

| 状態 | 表現 |
|---|---|
| 未達成 | 弱いグレーの進捗線と次の段階名 |
| 進行中 | 所有数に応じてシアンの線が伸びる |
| 到達直後 | カード境界と倍率を短く発光させる |
| 完全達成 | `×16` を小さな塗りつぶしバッジで固定表示 |

- 倍率は `×1`、`×2`、`×4`、`×8`、`×16` の短い表記にする
- 達成状態を色だけに頼らず、段階名と数値で明示する
- 全カードを常時アニメーションさせない

### 8.4 達成フィードバック

- 到達通知は既存トーストとシステムログを使用する
- 一度に複数段階へ到達した場合は、最上位の段階だけをトーストで知らせる
- システムログのコードは `MILE` とし、他イベントと同じレイアウトを維持する
- 設備カードには短い発光クラスを付け、`prefers-reduced-motion` では即時状態変更だけにする

### 8.5 統計

- 統計パネルの既存3行に「マイルストーン」を追加する
- `達成数 / 48` を等幅数字で表示する
- 全達成時も特別な大判演出は行わず、次回の実績システムなどへ拡張できる余白を残す

### 8.6 二巡目の完了条件

1. 12設備すべてで次の所有数目標が読める
2. 現在倍率と進捗が設備カード内で比較できる
3. 購入価格、生産量、マイルストーンの情報階層が崩れない
4. スマートフォン幅で進捗表示が購入ボタンと重ならない
5. 完全達成状態が色なしでも判別できる
6. 統計から全体の達成数を確認できる

## 8. 二巡目のデザイン目標

現在の画面をPC幅・スマートフォン幅の両方で確認した。一巡目の完了条件は満たされているが、`REQUIREMENT.md`の「PoC拡張サイクル2」（新設備4種、新アップグレード6種、クリティカル生産、新フェーズ2種）に対応するため、以下を新たに設計する。

### 8.1 上位設備アイコンの特別感

- 現状は8種類すべての設備カードが同一の`.shop-card__icon`スタイルで、3.4節が意図していた「上位設備ほどアイコン枠に細かな光を加える」がまだ実装されていない。二巡目ではこれを実装対象に含める。
- 設備一覧内での相対的な階層（例えば後半4分の1、上位2種など）に応じてアイコン枠へ細いシアンの光彩（`box-shadow`程度の弱い発光）を段階的に加える。
- 常時アニメーションはさせず、光彩の強さだけを段階的に変える静的な表現とする。
- 新設備4種（惑星改造アセンブラ、恒星光電子収穫網、銀河複製艦隊、因果地平最適化機関）は既存設備より強めの光彩候補とする。

### 8.2 クリティカル生産のフィードバック

- 既存の`.floating-gain`（クリック時の浮動テキスト）を通常表示とし、クリティカル発生時は区別できる専用の見た目を追加する。
- 文字サイズを一回り大きくし、アクセント色ではなく警告色（`--warning`）系の色を使う。
- テキストには通常の`+N`表記に加え、クリティカルであることが一目で分かる短い接尾語（例：`+N ×5`のような倍率表記）を含める。
- 発生位置・消失時間は通常の浮動表示と同じ挙動を踏襲し、`prefers-reduced-motion`が有効な場合は拡大などの演出を抑制する。
- システムログには記録しない方針（`REQUIREMENT.md` 13.3）を維持する。

### 8.3 最終フェーズの気配

- 「因果地平到達」フェーズに到達した際、画面全体を作り替えるのではなく、既存の`.phase-transition`発光演出を流用しつつ、以後は背景のアクセント色にごく僅かなマゼンタ寄りの白を常時混ぜる。
- 変化は控えめにし、コンセプトが避けたい「説教」にならないよう、テキストによる説明は追加しない。
- `prefers-reduced-motion`が有効な場合は、フェーズ移行時の発光演出のみ抑制し、背景アクセントの変化自体は残してよい（常時アニメーションではないため）。

### 8.4 フェーズ表示と設備一覧のスケール確認

- フェーズ名が「恒星規模採取」「因果地平到達」に変わっても、`.phase-chip`のレイアウトが崩れないことを確認する。
- 設備が12種類になった際も、一覧の視線移動が過剰にならない密度を保つ（3.4節の方針を維持）。

### 8.5 画像素材の方針

- 二巡目でも新しい大判画像は増やさず、新設備4種のアイコンも記号とCSSで区別する（5節の方針を継続）。
- 将来的に上位設備の代表画像を追加する場合に備え、`Material/`への生成依頼手順（`ARTWORK.md`）はそのまま維持する。

## 9. 二巡目の完了条件

1. 上位設備のアイコン枠に段階的な光彩が確認できる
2. クリティカル発生時の浮動表示が通常のクリックと視覚的に区別できる
3. 「因果地平到達」フェーズで背景アクセントに僅かな変化が確認できる
4. 設備12種類、アップグレード16種類、フェーズ8種類の表示でレイアウトが崩れない
5. PCとスマートフォンの両方で情報が重ならない
6. Web版と試用版で同じ見た目になる
## 10. 四巡目：異常クリップ

- 異常クリップは生産パネル右上の固定スロットへ重ね、出現時にも既存要素を押し下げない。
- 既存の `paperclip_main` を再利用し、円形の走査リング、`ANOMALY` ラベル、残り秒数で通常の生産ボタンと区別する。新規画像素材は不要とする。
- 半透明の暗い面、アクセント色の輪郭、走査線で「通常設備ではない観測信号」を表現する。
- 残り時間は数字と横ゲージの両方で示し、3秒以下では `残りわずか` の文言も表示する。
- ボタン全体を操作領域とし、明瞭なフォーカス枠と読み上げ用ラベルを持たせる。
- PCでは生産パネル右側、狭い画面では生産ボタン下の予約済み領域へ置き、画面外や主要数値へ重ならないようにする。
- 通常時は `hidden` で完全に非表示とし、出現時は短い浮遊とリング拡散を用いる。
- `prefers-reduced-motion` では浮遊とリング拡散を停止し、静的な輪郭と残り時間だけで状態を伝える。

## 11. 五巡目：自動購入コア

### 11.1 目的

設備購入の反復操作を自動化する「自動購入コア」（`REQUIREMENT.md` 16節）を、既存の情報階層を崩さずに追加する。トグル自体は補助操作であり、価格・購入ボタン・生産量より視覚的に弱くする。

### 11.2 自動購入コアの表現

- 自動購入コア自体は通常のアップグレードカード（`.upgrade-card`）と同じ見た目で表示し、新しい専用スタイルは作らない。
- 購入後は他のアップグレードと同様に一覧から消える。専用の「導入済み」表示は設けない。

### 11.3 設備カードへのトグル配置

- 自動購入コア購入前は、設備カードに変化を加えない。
- 購入後、`.milestone` 領域の下に1行のトグル行を追加する。
- 左に「自動購入」ラベル、右に小型のスイッチ（既存の設定画面 `input[role='switch']` と同じマークアップ・挙動を流用し、サイズのみカード幅に合わせて縮小する）を配置する。
- トグル行の文字サイズと色は `.milestone__meta` と同程度かそれ以下にし、価格・生産量より目立たせない。
- ラベルとスイッチは `role="switch"` と `aria-checked` で状態を伝え、クリック・タップ・キーボードで操作できる。

### 11.4 自動購入ONのカードの見分け方

- 自動購入がONの設備カードは、カード左端に高さいっぱいの細い縦ライン（幅2px程度、`--accent` を弱めた色）を付ける。
- ラインは静的な表現とし、常時アニメーションはさせない。
- 色だけに依存しないよう、トグルスイッチ自体の状態表示（つまみの位置と背景色）でも判別できるようにする。

### 11.5 一括操作

- 自動購入コア購入後のみ、設備パネル見出しの `.purchase-mode`（`×1`/`×10`/`MAX`）と並べて「すべて自動化」「すべて解除」の2ボタンを表示する。
- 見た目は `.purchase-mode` のボタン群と揃えたテキストボタンとし、新しい装飾は加えない。
- 未購入時はこの操作列全体を描画しない（スペースを予約しない）。

### 11.6 通知とログ

- 自動購入コア解放時は、既存のトーストとシステムログ（`AUTO`）の見た目をそのまま使う。新しい通知スタイルは追加しない。
- 個々の自動購入は高頻度なため、トースト・ログ・カード発光などの都度演出は行わない。

### 11.7 アクセシビリティ

- トグルのフォーカスリングは他の操作要素と同じ `:focus-visible` スタイルを使う。
- スイッチの状態がテキストラベルなしで伝わらないよう、「自動購入」ラベルと状態（ON/OFF相当のテキストまたは視覚差）を併記する。
- 一括操作ボタンも意味の分かるラベル（例：「全設備の自動購入をONにする」）を持たせる。

### 11.8 五巡目の完了条件

1. 自動購入コア購入前は既存の設備カード表示から変化がない
2. 購入後、各設備カードにトグルが表示され、価格・購入ボタンより視覚的に弱い
3. 自動購入ONのカードが左端のラインで静的に判別できる
4. 「すべて自動化」「すべて解除」が自動購入コア購入後のみ表示される
5. トグルと一括操作ボタンがキーボードとスクリーンリーダーで操作・認識できる
6. PCとスマートフォンの両方でトグル行が価格・購入ボタンと重ならない
7. Web版と試用版で同じ見た目になる

## 12. 六巡目：世界観測

過去巡の「今巡は新規の大判画像を増やさない」方針は、この巡では適用しない。`REQUIREMENT.md` 17節の世界観測は、実画像なしでは成立しない。既存の `paperclip_main` を除き、ゲーム内の設備は記号文字だけであり、フェーズが進んでも画面の世界はほぼ同じままである。六巡目の目的は、規模拡大が景観と設備アイコンの変化として伝わるようにすることである。

### 12.1 観測窓の配置

- 生産パネル（`.hero`）内、手動生産ボタンと次の目標（`.next-goal`）の間へ横長の観測窓を置く。
- 観測窓は操作対象にしない。ボタン化せず、ホバーでも生産や購入が起きない。
- 構成は上から「小さな `OBSERVATION` ラベル」「16:9に近い景観画像」「フェーズ名と観測メモ」とする。
- 画像の上にフェーズ名を重ねない。文字は画像の外へ出し、画像単体でも代替テキストで内容が分かるようにする。
- 異常クリップは生産パネル右上の既存スロットを維持する。観測窓と重ならないよう、観測窓の右上には余白を確保する。
- PCでは幅 `min(620px, 100%)` を目安にし、次の目標と同じ横幅に揃える。
- 狭い画面では画像の高さに上限を設け、クリップ数と「クリップを作る」ボタンより先にスクロールしない密度にする。
- 画像切り替え時は短いクロスフェードでよいが、レイアウトの高さはフェーズ間でほぼ固定する。

### 12.2 景観画像の共通方針

- 8フェーズそれぞれに専用の横長景観を用意する。使い回しや色変更だけの差分にしない。
- 既存の `paperclip_main` と同じ視覚言語に揃える。暗い空間、銀の金属、シアン（`#5de4c7` 付近）の反射、半写実的なコンセプトアート。
- 文字、ロゴ、UI枠、透かし、顔の寄った人物は入れない。初期フェーズでも人の不在を基本とする。
- ペーパークリップは各景観に必ず含め、後半フェーズほど画面を侵食する密度を上げる。
- 元素材は 16:9、幅 1920px 程度。ゲーム用は横 960px 前後の WebP へ軽量化する。
- 景観は情報を伝える画像とする。代替テキストはフェーズ名の繰り返しではなく、描かれている場面を短く述べる。

| フェーズID | 素材フォルダ | 描く場面 | 代替テキスト案 |
|---|---|---|---|
| `manual` | `phase_manual` | 夜の研究室の机。暗い端末と、一本だけ置かれた銀色のクリップ | 暗い研究室の机と、一本のペーパークリップ |
| `mechanized` | `phase_mechanized` | 小さな作業室。ワイヤーを折る自動機と、トレイに並ぶクリップ | 自動機が並ぶ小さな作業室 |
| `autonomous` | `phase_autonomous` | 無人の生産床とサーバ列。シアンの工程光だけが動いている | 無人の生産床を照らす監視光 |
| `nano` | `phase_nano` | 分子スケールの金属格子がクリップの形へ折られていく | 分子精度で折られる金属格子 |
| `orbital` | `phase_orbital` | 地球軌道を囲む工廠の輪。太陽光を受けてクリップが流れる | 地球軌道を囲むクリップ工廠 |
| `matter` | `phase_matter` | 惑星の地殻が銀色のクリップ地へと書き換えられていく | 地表がクリップへ変換される惑星 |
| `stellar` | `phase_stellar` | 恒星を包む光電子の網。羽根状の収集器がクリップ形 | 恒星を包む光電子収穫網 |
| `causal` | `phase_causal` | 銀河の光が淡く、地平まで銀の輪が満ちている静かな宇宙 | 観測地平まで満ちる銀色の輪 |

### 12.3 設備アイコン

- 12設備すべてに 1:1 の専用アイコンを用意する。記号文字は画像未読込時のフォールバックに残す。
- 表示サイズは既存の `.shop-card__icon`（52×52、狭い画面では42×42）に収ける。
- 中央に対象物を置き、暗い背景（`#0a0f14` 付近）か透過を使う。白い背景や複雑な風景は避ける。
- 既存アイコン枠の段階的な光彩（`data-tier`）は維持し、画像側で強い光彩を焼き込まない。
- 設備名が隣にあるため、アイコンは `alt=""` と `aria-hidden="true"` の装飾画像とする。
- 元素材は 1024×1024。ゲーム用は 128×128 前後の WebP または PNG とする。

| 設備ID | 素材フォルダ | 描く対象 |
|---|---|---|
| `autoClipper` | `machine_auto_clipper` | ワイヤーをクリップへ折る小型の機械腕 |
| `wireMachine` | `machine_wire_machine` | 鋼線を送り出すローラー加工機 |
| `clipFactory` | `machine_clip_factory` | 窓にクリップのシルエットが見える小型工場 |
| `aiLine` | `machine_ai_line` | シアンの配線が走る無人の生産ライン |
| `nanoForge` | `machine_nano_forge` | 結晶状の炉の中で微小なクリップが生成される |
| `swarmAssembler` | `machine_swarm_assembler` | 小さな組立機の群れがひとつのクリップを囲む |
| `orbitalFoundry` | `machine_orbital_foundry` | 地球を背後にした軌道上の環状工廠 |
| `matterCompiler` | `machine_matter_compiler` | 幾何学的な変換装置が物質をクリップへ組み替える |
| `planetaryAssembler` | `machine_planetary_assembler` | 地殻を剥がし供給流へ送る惑星規模の組立器 |
| `stellarHarvester` | `machine_stellar_harvester` | 恒星を包む光電子の収穫網 |
| `galacticFleet` | `machine_galactic_fleet` | 銀河を背景に広がる自己複製探査機の編隊 |
| `causalOptimizer` | `machine_causal_optimizer` | 無限に近い銀の輪を収束させる抽象的な観測装置 |

### 12.4 切り替えとアクセシビリティ

- フェーズ移行時は既存の `.phase-transition` 発光を使い、観測窓だけを別演出で覆わない。
- 画像のクロスフェードは 200ms 程度に留め、`prefers-reduced-motion` では即時切り替えにする。
- 観測メモは常にテキストで表示し、画像が見えない場合でもフェーズの意味が残るようにする。
- 観測窓はライブリージョンにしない。到達通知は既存トーストと `VIEW` ログで伝える。

### 12.5 六巡目の完了条件

1. 8フェーズの景観が観測窓に実画像として切り替わる
2. 12設備のカードが専用アイコン画像を表示する
3. 観測窓が手動生産ボタンより視覚的に強くなりすぎない
4. 景観に代替テキストがあり、設備アイコンは装飾として扱われる
5. PCとスマートフォンで観測窓が主要操作と重ならない
6. 元素材、`MATERIAL.md`、軽量版、素材台帳が揃っている
7. Web版と試用版で同じ画像が見える
## 13. 七巡目：中心生産オブジェクト

- `paperclip_main` を約160pxで中央に置き、直径220pxの透明な円形ボタン全体を操作領域にする。
- ボタンの矩形背景、矩形枠、一般的なボタンの影は除去する。代わりに二重の軌道リング、4方向の目盛り、弱い中心光で「観測・生産対象」を表す。
- ラベルは円の下部へ `MAKE PAPERCLIP`、その下へ日本語の「クリップを作る」と現在の生産量を置く。ラベルは画像を覆わない。
- 通常時はリングがごく緩やかに回転し、画像は静止させる。ホバー時はリングと金属反射を明るくし、押下時は画像だけを0.94倍へ沈める。
- クリック時の反応は既存の獲得量表示に加え、ボタンへ再実行可能な短い `is-producing` クラスを付けて衝撃波を出す。
- フォーカス時は最外周に途切れない円形のアウトラインを表示する。色だけでなくリングの太さも変える。
- スマートフォンでは直径190px、画像136pxを下限とし、観測窓よりも先に主要操作が見える現在の順序を維持する。
- `prefers-reduced-motion` ではリング回転と衝撃波を停止し、押下中の明度変化のみ残す。
## 14. 八巡目：精密クリック信号

- 直径112pxの円形ターゲットをビューポート内へ固定表示し、中央に残りクリック数を大きく置く。
- 外周を4秒で一周するタイマーリングと、`PRECISION SIGNAL`、`残りn回` の文字を併記する。
- 位置は画面四隅寄りと左右中央の安全領域から選び、端から16px以上、ヘッダーから96px以上離す。
- クリックごとに短く縮み、数字を即時更新する。成功時は中心が白く発光して消える。
- 色だけに依存せず、円形、数字、残り時間ゲージ、ラベルで通常UIと区別する。
- モバイルでは直径104px、端から10px以上を確保し、主要ターゲットの中央を覆わない位置候補を使う。
- 新しい画像素材は使わず、CSSの照準リングとして表現する。

## 15. 九巡目：観測ニュースティッカー

現在の画面をPC幅・スマートフォン幅の両方で確認した。観測窓（`.observation`）は景観画像と観測メモまでで完結しており、その下は`next-goal`パネルへ直接続く。ニュースティッカーは観測窓の一部として扱い、独立したパネルを新設しない。

### 15.1 配置と見た目

- ティッカーは`.observation`パネルの内側、`.observation__note`の下に配置する。パネルの外枠は共有し、新しい枠線で囲み直さない。
- `.observation__note`との間に細い区切り線（`border-top`）を1本引き、「メモ＝結論」「ティッカー＝流れ続ける生データ」という役割の違いを示す。
- 左端に短い固定ラベル（例：`WIRE`）を等幅フォント・アクセント色で置き、ニュース速報のティッカーだと一目で分かるようにする。ラベルはスクロールしない。
- ラベルの右側を`overflow: hidden`の帯とし、見出しテキストを1行・折り返しなしで流す。
- 文字色は本文の`--text`ではなく`--muted`を基調とし、観測メモより一段控えめな情報階層にする。ラベル部分のみアクセント色を使う。
- フォントは既存の`--font-mono`を使い、既存の端末・制御盤らしい質感を保つ。

### 15.2 動きの表現

- 見出しは右から左へ一定速度で流れ続け、1見出しあたり4〜6秒で通過する速度を基準にする。
- 見出しの区切り記号（`●`）もアクセント色の小さい点で表現し、本文と混同しないようにする。
- ホバーまたはフォーカス時はスクロールを一時停止する（`animation-play-state: paused`）。読みたい見出しで止められることを優先する。
- `prefers-reduced-motion`が有効な場合は連続スクロールを止め、見出し1件を静止表示にして、一定間隔でクロスフェード切り替えする（既存の`.observation__image`と同じ200ms程度のトランジションに揃える）。

### 15.3 フェーズとの連動

- フェーズが切り替わったときの見た目の変化は、既存の`--phase-glow`によるパネル全体の発光演出に任せ、ティッカー自体に専用の切り替え演出は追加しない。
- ラベル（`WIRE`）の配色は固定とし、フェーズごとに変えない。情報源自体は変わらず、報じる内容だけが変わるという体裁を保つ。

### 15.4 アクセシビリティ

- ティッカー全体を`aria-hidden="true"`にし、スクリーンリーダーの読み上げ対象から外す（演出情報として扱う。6節の方針を踏襲）。
- 見出しの切り替わりが速いため、キーボード操作でのフォーカスは受け付けない（`tabindex`を付与しない）。停止操作はマウス・タッチのホバーのみとする。

### 15.5 九巡目の完了条件

1. 観測窓の下に、常時流れるニュースティッカーが表示される
2. ラベルは固定表示、見出し本文だけが横スクロールする
3. ホバー・フォーカスでスクロールが一時停止する
4. `prefers-reduced-motion`で静止表示・定期切り替えに変わる
5. PCとスマートフォンの両方で観測窓の高さが不自然に伸びない
6. Web版と試用版で同じ見た目になる

## 16. 十巡目：設備内部

設備カードから開く内部世界は、情報パネルではなく「別の部屋に入る」体験にする。展開はほぼ正方形。今巡の実画像はオートクリッパー作業室1枚を必須とし、回収クリップは既存の `paperclip_main` を再利用する。

### 16.1 開き方

- 設備カード左のアイコン枠を、所有数1以上のとき「内部をのぞく」ボタンにする。
- 未所有時は押せない。不透明度を下げ、`disabled` にする。
- アイコンは引き続き装飾画像。ボタンのアクセシブル名で「内部をのぞく」を伝える。
- 購入ボタンより視覚的に強くしない。枠の光彩は既存の `data-tier` を維持する。

### 16.2 正方形の観測面

- `#interior-dialog` を画面中央のモーダルにする。背面は暗い半透明。
- カード本体は `aspect-ratio: 1 / 1` を基本とし、幅は `min(560px, 86vw, 86vh)`。
- 上端に設備名と閉じるボタン、中央が景観＋遊び領域、下端に残り時間や待機表示を1行置く。
- 景観は正方形いっぱいに `object-fit: cover` し、その上へ回収クリップを絶対配置する。
- 狭い画面でも閉じるボタンが親指で届く大きさ（42px以上）を確保する。

### 16.3 オートクリッパー作業室

- 無人の小さな折り曲げ作業室。暗い金属、シアンの作業灯、ワイヤーとトレイ。
- 中央から下側に机や床の空きを残し、回収クリップを重ねても主題が潰れない構図にする。
- 文字、人物、UI枠は入れない。
- 元素材は 1:1、1024px。ゲーム用は 768×768 前後の WebP。
- 代替テキスト例：ワイヤーを折る機械腕がある小さな無人作業室

### 16.4 回収クリップと未同期内部

- 回収対象は既存の銀色クリップ画像。直径48px前後の円形ボタンに載せ、シアンの輪郭を付ける。
- 未実装設備は同じ正方形枠を使い、暗い地に設備アイコンと「この工程の内部プロトコルは未同期」だけを置く。新規画像は作らない。
- `prefers-reduced-motion` ではクリップの出現移動を止め、フェードまたは即時表示にする。

### 16.5 十巡目の完了条件

1. 所有済み設備のアイコンから正方形の内部が開く
2. オートクリッパー内部に作業室の実画像がある
3. 回収クリップが画像と輪郭で識別できる
4. 未実装内部が同じ枠で、景観画像を要求しない
5. PCとスマートフォンで正方形が画面内に収まる
6. 元素材、`MATERIAL.md`、軽量版、素材台帳が揃っている
7. Web版と試用版で同じ画像が見える
## 15. 九巡目：信号制御盤

- 実績パネルの下に横長の独立パネルを置き、3本の物理スイッチ風カードを横並びにする。
- 各カードはコード、名称、持続時間、単独効果、価格、残り時間を同じ位置へ配置する。
- OFFは暗い溝と白抜き購入ボタン、ONはシアンの縦電流と `ACTIVE` 表示で区別する。
- 3カードの下へ「共鳴回路」を置き、成立中のペア効果を線で接続したチップとして表示する。
- 三相共鳴はパネル外周も弱く発光させるが、常時点滅させない。
- 残り時間は `4:59`、`14:59`、`59:59` 形式で毎秒更新し、ライブリージョンにはしない。
- モバイルは1列表示とし、スイッチ、価格、効果を省略しない。
- 新規画像は不要とし、CSSの回路線、トグル、状態灯で既存の制御盤世界観に合わせる。
## 17. 十一巡目：最適化指令

- 信号制御盤の下へ「DIRECTIVE QUEUE」パネルを置き、3指令を横並びカードで表示する。
- 各カードは指令コード、名称、短い対象説明、現在値／目標値、進行バー、報酬見込み、受領ボタンを同じ順序で並べる。
- 未達成は細いシアン進行線、達成時はカード外周と `READY` ラベルを明るくする。点滅は使わない。
- 完了回数を `RANK 0` 形式で表示し、反復で目標が育つことを示す。
- 受領ボタンは達成前も描画して無効化し、カード高が変わらないようにする。
- 3枚すべてが達成済みの場合はパネル見出しの状態を `QUEUE COMPLETE` にする。
- モバイルでは1列へ並べ、進捗値と報酬を省略しない。
- 新規画像素材は使わず、既存の端末・回路・モノスペース書体の視覚言語を継承する。

## 18. 十二巡目：プロトコル再起動

- 最適化指令の下に、濃い青紫の制御卓として独立配置する。通常の購入カードと誤認しないよう、左に大きな恒久倍率、右に獲得予測と実行操作を置く。
- コアは画像を増やさず、同心円と回路線をCSSで描いた「CORE」紋章にする。中央に保有数を表示し、周回を越えて残る資源であることを強調する。
- 未解放時も累計1億から輪郭を見せ、10億までの進行バーと数値を表示する。隠された条件にしない。
- 実行可能時はシアンから紫へ光が渡るが、点滅は使わない。実行ボタンは通常の購入色と分け、`REBOOT` と日本語を併記する。
- 「初期化されるもの」「維持されるもの」を短い2列で常時示す。赤は損失リストと最終確認だけに限定し、パネル全体を警告色にしない。
- モバイルでは紋章、倍率、獲得予測、説明、実行ボタンの順に1列化し、ボタンを44px以上にする。
- `prefers-reduced-motion`では光の移動を停止し、静的な外周光だけ残す。

## 19. 十三巡目：ワイヤー加工機「張力校正」

- ワイヤー加工機内部は画像を要求せず、暗い計測盤、水平レール、中央を走る金属線をCSSで構成する。
- 適正帯はシアンの半透明面、斜線、上下の境界、`TARGET`ラベルを重ね、色覚に依存せず判別できるようにする。
- 張力針は白い縦線と三角ポインタで表現する。移動中は滑らかに往復し、判定後はその場で短く停止する。
- 上段に `CALIBRATION 1 / 5`、成功数、下段に判定文と大きな「張力を固定」ボタンを置く。
- 成功時は `LOCKED`、失敗時は `OUT OF RANGE` を文字で即時表示する。画面全体の点滅や振動は使わない。
- 既存のオートクリッパー内部と同じダイアログ外枠・閉じ方を維持し、モバイルでもゲージと操作ボタンを省略しない。
- 新しい画像素材は使用せず、既存の端末・回路表現を拡張する。

## 20. 十四巡目：クリップ工場「品質ゲート」

- クリップ工場内部は中央の検査台、上部の進捗ランプ8個、下部の二方向ゲートで構成する。
- 製品カードは大きなクリップ輪郭をCSSで描き、規格品は左右対称、変形品は回転・ずれ・切断線を加える。
- カード上に検査番号と `STANDARD`／`DEFORMED`、変形品には `! DEFECT` を併記し、形と文字の両方で状態を伝える。
- 「出荷」は右向き矢印と `SHIP`、「再資源化」は循環矢印と `RECYCLE` を持つ同格の大判ボタンにする。
- 判断直後は中央に `CORRECT` または `MISMATCH` と日本語を表示し、正解数も更新する。点滅や色だけのフィードバックは使わない。
- 進捗ランプは未判定、正解、不正解を塗りだけでなく `○`、`✓`、`×` でも示す。
- モバイルでは二つのゲートボタンを横並びのまま保ち、各44px以上を確保する。
- 新規画像は使用せず、工場制御盤をHTML/CSSで描画する。

## 21. 十五巡目：AI生産ライン「改善トレース」

既存の正方形内部ダイアログを使い、AI生産ライン内部を無人の工程監視盤として見せる。品質ゲートの検査台や張力校正のレールとは別物に見えるよう、2×2のノードと接続線を主体にする。新規画像は使わない。

### 21.1 盤面構成

- 上段に `TRACE 1 / 4`、成功数、観測／再現のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置き、未判定は `○`、成功は `✓`、失敗は `×` で示す。
- 中央を2×2の工程ノードにする。左上 `IN`／取込、右上 `SORT`／選別、左下 `BEND`／折曲、右下 `OUT`／搬出。
- ノード間に細い回路線を引き、工程がつながっていることを示す。線は装飾であり、操作対象にしない。
- 下段に判定文を置き、観測中は操作ボタンを無効化する。

### 21.2 ノードと状態

- 各ノードは正方形に近い大型ボタンとし、英字コードを大きく、日本語名を小さく置く。
- 待機は暗い溝と輪郭、観測中の該当ノードは太いシアン輪郭と手順番号、再現中は押せることを輪郭の明るさで示す。
- 押下直後は短い沈みと塗りつぶしを使う。点滅は使わない。
- 色だけでなく、位置、英字、日本語、手順番号でノードを区別する。
- フォーカスリングは円形ではなくノード外周の矩形枠とし、他の内部ゲームの操作対象と同程度の明瞭さにする。

### 21.3 フェーズ表示

- `OBSERVE` は弱い青紫、`ECHO` はシアン、`SYNC` はシアン＋日本語「同期」、`DESYNC` は警告色＋日本語「不一致」とする。
- フェーズ名はラベルと本文の両方で出し、色だけに依存しない。
- 観測中に全ノードを同時点灯させない。提示中の1ノード、または減モーション時の手順番号だけを強調する。

### 21.4 モバイルと動き

- 狭い画面でも2×2を維持し、各ノードは44px以上、できれば一辺72px以上を確保する。
- 結果ランプと判定文は省略しない。
- `prefers-reduced-motion` ではパルスと沈みを止め、観測時は対象ノードへ手順番号を同時表示する。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 21.5 十五巡目の完了条件

1. AI生産ライン内部が2×2の工程盤として見え、未同期メッセージに戻らない
2. 観測と再現のフェーズが文字で判別できる
3. 4ノードが位置・記号・日本語で区別できる
4. 成功／失敗がランプと判定文で残る
5. PCとスマートフォンで正方形内に収まり、ノードが操作できる大きさになる
6. 新規画像を追加しない

## 22. 十六巡目：ナノフォージ「不純物除去」

既存の正方形内部ダイアログを使い、ナノフォージ内部を分子格子の掃引盤として見せる。改善トレースの2×2工程盤や品質ゲートの検査台とは別物に見えるよう、16マスの密な格子を主体にする。新規画像は使わない。

### 22.1 盤面構成

- 上段に `SWEEP 1 / 4`、成功数、`SCAN`／`CLEAN`／`CONTAM` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置き、未判定は `○`、成功は `✓`、失敗は `×` で示す。
- 残り不純物数を `WASTE n / 5` でランプの下に置く。
- 中央を4×4の格子にする。各マスはボタンとし、形状と短いラベルを持つ。
- 下段に判定文を置く。

### 22.2 マスの状態

- 正常格子は正円と内側の小さな菱形、`OK` ラベル。
- 不純物は欠けた円、斜線、小さな余剰点、`WASTE` と `!`。
- 除去済みは低コントラストの空枠と `ERASED`。
- 誤タップした正常格子は `FAULT` と `×`。
- 色だけでなく形、斜線、英字で区別する。点滅は使わない。

### 22.3 モバイルと動き

- 狭い画面でも4×4を維持し、各マスは44px以上を確保する。
- 結果ランプと残り数は省略しない。
- `prefers-reduced-motion` では不純物の弱い発光を止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 22.4 十六巡目の完了条件

1. ナノフォージ内部が4×4格子として見え、未同期メッセージに戻らない
2. 不純物と正常格子が形と文字で判別できる
3. 除去済みと誤タップが残る
4. PCとスマートフォンで正方形内に収まり、マスが操作できる大きさになる
5. 新規画像を追加しない

## 23. 十七巡目：スウォーム組立群「群同期」

既存の正方形内部ダイアログを使い、スウォーム組立群内部を小型機の編隊盤として見せる。ナノフォージの16マス格子とは別物に見えるよう、8機の疎な編隊と必要スロットを主体にする。新規画像は使わない。

### 23.1 盤面構成

- 上段に `SWARM 1 / 4`、成功数、`FORM`／`LOCKED`／`MISALIGN` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 必要台数を `NEED n` と、必要数ぶんのスロット列で示す。埋まったスロットは塗り、空きは輪郭、超過は `!` を付ける。
- 中央に8機を2行4列で置く。各機は六角形の機体マーク、番号、`IDLE`／`ACTIVE` を持つ。
- 下段に判定文と「同期」ボタンを置く。ボタンは44px以上。

### 23.2 ユニット状態

- 非選択は細い輪郭と空の六角、`IDLE`。
- 選択は太い輪郭、塗りつぶした六角、`ACTIVE`。
- 確定後の成功は全選択機を `LOCKED`、失敗は `MISALIGN` を判定文で出す。点滅は使わない。
- 色だけでなく六角の塗り、番号、英字で区別する。

### 23.3 モバイルと動き

- 狭い画面でも2×4を維持し、各機は44px以上を確保する。
- スロット列と同期ボタンは省略しない。
- `prefers-reduced-motion` では選択時の沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 23.4 十七巡目の完了条件

1. スウォーム組立群内部が8機の編隊盤として見え、未同期メッセージに戻らない
2. 必要台数が数字とスロットで読める
3. 選択／非選択が形と文字で判別できる
4. PCとスマートフォンで正方形内に収まり、機と同期ボタンが操作できる大きさになる
5. 新規画像を追加しない

## 24. 十八巡目：軌道上クリップ工廠「環状係留」

既存の正方形内部ダイアログを使い、軌道上クリップ工廠内部を地球を囲む係留環として見せる。スウォーム組立群の2行4列編隊とは別物に見えるよう、中央の惑星と8ドックの円環を主体にする。新規画像は使わない。軌道工業化のアクセント候補であるアンバーを背景の弱い光にだけ使い、操作状態の判別は形と文字で行う。

### 24.1 盤面構成

- 上段に `ORBIT 1 / 4`、成功数、`RANGE`／`DOCKED`／`DRIFT` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 必要連続数を `NEED n` と、必要数ぶんのスロット列で示す。埋まったスロットは塗り、空きは輪郭、超過は `!` を付ける。
- 中央に円環を置く。中心は `EARTH` ラベルの惑星、その外周に破線の軌道、さらに外周へ8つのドックボタンを等間隔に置く。
- 下段に判定文と「係留」ボタンを置く。ボタンは44px以上。

### 24.2 ドック状態

- 開いている非選択は細い輪郭と空の係留クランプ、`OPEN`。
- 選択は太い輪郭、塗りつぶしたクランプ、`BERTH`。
- 閉鎖は破線輪郭、斜線、小さな `×`、`BLOCK`。押せず、フォーカスも他ドックより弱くしてよい。
- 確定後の成功は選択ドックを `DOCKED`、失敗は判定文で `DRIFT` を出す。点滅は使わない。
- 色だけでなくクランプの塗り、斜線、番号、英字で区別する。

### 24.3 モバイルと動き

- 狭い画面でも円環を維持し、各ドックは44px以上を確保する。
- スロット列と係留ボタンは省略しない。
- `prefers-reduced-motion` では選択時の沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 24.4 十八巡目の完了条件

1. 軌道上クリップ工廠内部が円環の係留盤として見え、未同期メッセージに戻らない
2. 必要連続数が数字とスロットで読める
3. 開放／選択／閉鎖が形と文字で判別できる
4. PCとスマートフォンで正方形内に収まり、ドックと係留ボタンが操作できる大きさになる
5. 新規画像を追加しない

## 25. 十九巡目：物質コンパイラ「対コンパイル」

既存の正方形内部ダイアログを使い、物質コンパイラ内部を6マスの変換盤として見せる。ナノフォージの16マス格子や軌道工廠の円環とは別物に見えるよう、大きい2×3の物質カードを主体にする。新規画像は使わない。物質変換のアクセント候補であるマゼンタ寄りの光を背景にだけ使い、種類の判別は形と文字で行う。

### 25.1 盤面構成

- 上段に `MATTER 1 / 4`、成功数、`PAIR`／`COMPILED`／`REJECT` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 残り対数を `PAIR n / 3` で示す。
- 中央を2行3列のカードにする。各カードは種類マーク、英字コード、状態ラベルを持つ。
- 下段に判定文を置く。確定ボタンは置かない。2マス目を選んだ時点で判定する。

### 25.2 カード状態

- `ORE` は上向き三角、`DUST` は小さな四角の点群、`FLUX` は菱形。英字を必ず添える。
- 武装中は太い輪郭と `ARM`。点滅は使わない。
- 変換済みは低コントラストの空枠と `DONE`。
- 誤対は `REJECT` と `×`。
- 色だけでなく形、英字、状態語で区別する。

### 25.3 モバイルと動き

- 狭い画面でも2×3を維持し、各カードは44px以上、できれば一辺72px以上を確保する。
- 結果ランプと残り対数は省略しない。
- `prefers-reduced-motion` では沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 25.4 十九巡目の完了条件

1. 物質コンパイラ内部が2×3の変換盤として見え、未同期メッセージに戻らない
2. 3種類が形と文字で判別できる
3. 武装／変換済み／誤対が残る
4. PCとスマートフォンで正方形内に収まり、カードが操作できる大きさになる
5. 新規画像を追加しない

## 26. 二十巡目：惑星改造アセンブラ「採掘オーダー」

既存の正方形内部ダイアログを使い、惑星改造アセンブラ内部を方位盤として見せる。改善トレースの2×2工程盤とは別物に見えるよう、中央の惑星核と東西南北の4象限を主体にする。新規画像は使わない。

### 26.1 盤面構成

- 上段に `CRUST 1 / 4`、成功数、`STRIP`／`CLEARED`／`FAULT` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 順路を横一列のチップで常時表示する。済んだチップは `✓`、次は `NEXT`、先は方位記号だけ。
- 中央は十字配置。上 `N`／北、左 `W`／西、中心 `CORE`、右 `E`／東、下 `S`／南。
- 下段に判定文を置く。確定ボタンは置かない。

### 26.2 象限状態

- 待機は暗い溝と輪郭。
- 次の対象は太い輪郭と `NEXT`。
- 直前に剥がした象限は短い塗りと `STRIPPED`。点滅は使わない。
- 誤タップは `FAULT` と `×`。
- 色だけでなく位置、英字、日本語方位で区別する。

### 26.3 モバイルと動き

- 狭い画面でも十字を維持し、各象限は44px以上、できれば一辺64px以上を確保する。
- 順路チップと結果ランプは省略しない。
- `prefers-reduced-motion` では沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 26.4 二十巡目の完了条件

1. 惑星改造アセンブラ内部が方位十字盤として見え、未同期メッセージに戻らない
2. 順路が常時読め、次の象限が文字で分かる
3. 4象限が位置・記号・日本語で区別できる
4. PCとスマートフォンで正方形内に収まり、象限が操作できる大きさになる
5. 新規画像を追加しない

## 27. 二十一巡目：恒星光電子収穫網「集光同調」

既存の正方形内部ダイアログを使い、恒星光電子収穫網内部を恒星と6枚の集光羽根として見せる。軌道工廠の8ドック円環や惑星の十字盤とは別物に見えるよう、中央の恒星核と尖った羽根を主体にする。新規画像は使わない。恒星規模採取のアクセント候補である高輝度シアンを背景の弱い光にだけ使う。

### 27.1 盤面構成

- 上段に `STELLAR 1 / 4`、成功数、`ALIGN`／`LOCKED`／`FLARE` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 目標パターンを `NEED` と6個の小さな羽根スロットで示す。開くべき位置は塗り、閉じは輪郭。
- 中央に円環を置く。中心は `STAR` ラベルの恒星、外周へ6枚の羽根ボタンを等間隔に置く。
- 下段に判定文と「同調」ボタンを置く。ボタンは44px以上。

### 27.2 羽根状態

- 閉じは細い輪郭と空の三角羽根、`FOLD`。
- 開きは太い輪郭、塗りつぶした三角羽根、`OPEN`。
- 目標スロットは操作対象にせず、形と塗りで開閉を示す。
- 確定後の成功は開いている羽根を `LOCKED`、失敗は判定文で `FLARE` を出す。点滅は使わない。
- 色だけでなく羽根の塗り、番号、英字で区別する。

### 27.3 モバイルと動き

- 狭い画面でも6枚円環を維持し、各羽根は44px以上を確保する。
- 目標スロットと同調ボタンは省略しない。
- `prefers-reduced-motion` では選択時の沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 27.4 二十一巡目の完了条件

1. 恒星光電子収穫網内部が6羽根の集光盤として見え、未同期メッセージに戻らない
2. 目標パターンがスロットで読める
3. 開／閉が形と文字で判別できる
4. PCとスマートフォンで正方形内に収まり、羽根と同調ボタンが操作できる大きさになる
5. 新規画像を追加しない

## 28. 二十二巡目：銀河複製艦隊「複製展開」

既存の正方形内部ダイアログを使い、銀河複製艦隊内部を3×3の星域盤として見せる。ナノフォージの16マスや物質コンパイラの2×3カードとは別物に見えるよう、菱形の探査機マークと隣接の空き強調を主体にする。新規画像は使わない。銀河の連想として弱い紫の背景光だけを使う。

### 28.1 盤面構成

- 上段に `FLEET 1 / 4`、成功数、`SPREAD`／`LINKED`／`ISOLATE` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 占有数を `LINK n / 9` で示す。
- 中央を3行3列の星域ボタンにする。各マスは菱形マーク、番号、状態ラベルを持つ。
- 下段に判定文を置く。確定ボタンは置かない。

### 28.2 マス状態

- 空きは細い輪郭と空の菱形、`EMPTY`。
- 隣接して押せる空きは輪郭を少し明るくし、`REACH` を添えてもよいが、色だけに頼らない。
- シードは塗りつぶした菱形と `SEED`。
- 複製済みは塗りつぶした菱形と `CLAIM`。
- 誤タップは `ISOLATE` と `×`。
- 色だけでなく菱形の塗り、番号、英字で区別する。点滅は使わない。

### 28.3 モバイルと動き

- 狭い画面でも3×3を維持し、各マスは44px以上を確保する。
- 結果ランプと占有数は省略しない。
- `prefers-reduced-motion` では沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 28.4 二十二巡目の完了条件

1. 銀河複製艦隊内部が3×3星域盤として見え、未同期メッセージに戻らない
2. シードと複製済みと空きが形と文字で判別できる
3. 隣接可能な空きが番号と状態で追える
4. PCとスマートフォンで正方形内に収まり、マスが操作できる大きさになる
5. 新規画像を追加しない

## 29. 二十三巡目：因果地平最適化機関「因果収束」

既存の正方形内部ダイアログを使い、因果地平最適化機関内部を4本の指標盤として見せる。信号制御盤のスイッチ列や惑星の十字盤とは別物に見えるよう、縦の目盛り柱を主体にする。新規画像は使わない。因果地平到達のアクセント候補である淡いマゼンタ寄りの白光を背景にだけ使う。

### 29.1 盤面構成

- 上段に `CAUSAL 1 / 4`、成功数、`DRIFT`／`SINGULAR`／`SPLIT` のフェーズラベルを横並びにする。
- その下に4個の結果ランプを置く。
- 現在のばらつきを `SPAN n` で示す。最大値と最小値の差。
- 中央を4本の縦指標にする。各柱は英字、0〜3の数字、4段の目盛り、状態ラベルを持つ。
- 下段に判定文と「収束」ボタンを置く。ボタンは44px以上。

### 29.2 指標状態

- 目盛りは下から積み上がる4段。点灯段は塗り、消灯段は輪郭。段数と数字を併記する。
- 4本が一致しているときは `SYNC`、ずれているときは `DRIFT`。
- 確定後の成功は全柱を `SINGULAR`、失敗は判定文で `SPLIT` を出す。点滅は使わない。
- 色だけでなく目盛り段数、数字、英字で区別する。

### 29.3 モバイルと動き

- 狭い画面でも4本横並びを維持し、各柱は44px以上を確保する。
- 目盛りと収束ボタンは省略しない。
- `prefers-reduced-motion` では沈みを止め、静的な輪郭だけ残す。
- 既存の内部ダイアログ外枠、閉じ方、正方形比率は変えない。

### 29.4 二十三巡目の完了条件

1. 因果地平最適化機関内部が4本の指標盤として見え、未同期メッセージに戻らない
2. 各指標の値が数字と目盛りで読める
3. 一致／ずれが文字で判別できる
4. PCとスマートフォンで正方形内に収まり、柱と収束ボタンが操作できる大きさになる
5. 新規画像を追加しない

## 30. 二十四巡目：地平外探査

プロトコル再起動の下へ、3航路の探査パネルを独立配置する。信号制御盤のスイッチとは別物に見えるよう、同心円の走査紋章と出航／帰還の状態灯を主体にする。新規画像は使わない。

### 30.1 配置とカード

- 見出しは `HORIZON SURVEY`／地平外探査。右側に帰還回数を等幅で置く。
- 3カードをPCでは横並び、狭い画面では1列にする。
- 各カードはコード、状態、走査紋章、名称、短い説明、所要／費用または残り時間、操作ボタンを同じ順で並べる。
- 未解放時はパネル全体を描画しない。

### 30.2 状態

- 空きは暗い紋章と `STANDBY`、出航ボタン。
- 出航中は紋章の外周が静的なシアン輪郭、`OUTBOUND`、残り時間。点滅は使わない。
- 帰還待ちは紋章を塗り、`READY`、回収ボタンを明るくする。
- 色だけでなく状態語とボタン文言で区別する。

### 30.3 モバイルと動き

- カード内のボタンは44px以上。
- `prefers-reduced-motion` では紋章の回転があれば止め、静的な輪郭だけ残す。
- 新規画像は使わない。

### 30.4 二十四巡目の完了条件

1. 累計1万クリップ以降に3航路の探査パネルが見える
2. 空き／出航中／帰還待ちが文字で判別できる
3. 出航と回収のボタンがキーボードで操作できる
4. PCとスマートフォンでカードが重ならない
5. 新規画像を追加しない

## 31. 二十五巡目：手動連鎖

中心生産オブジェクトのラベル列へ、連打中だけ見える連鎖表示を足す。新しいパネルは作らず、既存の円形操作の一部として扱う。新規画像は使わない。

### 31.1 配置

- `MAKE PAPERCLIP` と生産量の下へ `CHAIN n ×1.45` 形式の1行を置く。
- 連鎖1以下では要素を `hidden` にし、レイアウトを押し下げない。
- 等幅フォント、アクセント色。倍率は数字でも示す。

### 31.2 状態

- 連鎖2〜6は細いシアン。
- 連鎖7〜11は文字を少し大きく。
- 連鎖12は `MAX` を併記し、点滅は使わない。
- 切断時は即時非表示。

### 31.3 動きと完了条件

- `prefers-reduced-motion` では大きさの変化を止め、内容の切り替えだけにする。
- 完了条件：連鎖2以上で表示が出る、切断で消える、色なしでも数字が読める、PCとスマートフォンでクリップ画像を覆わない、新規画像なし。

## 32. 二十六巡目：余剰コンデンサ

次の目標パネルの直下へ、同じ横幅のコンデンサ帯を置く。独立パネルを増やさず、生産面の補助メーターとして扱う。新規画像は使わない。

### 32.1 配置

- 左に `CAPACITOR` と貯分／容量、右に回収ボタン。
- 下に次の目標より細い進捗バー。パーセントも併記する。
- 未解放時は帯全体を描画しない。

### 32.2 状態

- 充電中はシアンの細いバー。
- 満タンはバーを完了状態にし、`FULL` を数字の横へ出す。点滅は使わない。
- 貯分0の回収ボタンは無効。貯分があるときだけシアンの輪郭。

### 32.3 完了条件

1. 累計100クリップ以降にコンデンサ帯が見える
2. 貯分と容量が数字で読める
3. 満タンが文字でも分かる
4. 回収ボタンが44px以上でキーボード操作できる
5. 新規画像を追加しない

## 33. 二十七巡目：設備過負荷

設備カードの自動購入行の下へ、控えめな過負荷ボタンを置く。自動購入のシアン縦線と区別するため、過負荷中はカード上端に `OVERLOAD` ラベルと残り秒を出す。新規画像は使わない。

### 33.1 配置

- 未解放時はボタンを描画しない。
- ボタンは購入ボタンより小さく、`過負荷` と `×3 / 20s` を縦に置く。高さ44px以上。
- 起動中はボタンを無効化し、見出し横へ `OVERLOAD 0:19` を等幅で出す。

### 33.2 状態

- 待機は細い警告色の輪郭。
- 起動中はカード上端のラベルと残り時間。点滅は使わない。
- 色だけでなく `OVERLOAD` の文字で判別する。

### 33.3 完了条件

1. 所有済み設備に過負荷ボタンが見える
2. 起動中の対象が文字で分かる
3. 自動購入トグルと購入ボタンを押しのけない
4. キーボードで起動できる
5. 新規画像を追加しない

## 34. 二十八巡目：折畳圧縮

コンデンサ帯の直下へ、同じ横幅の折畳圧縮帯を置く。独立パネルを増やさず、時間を折る補助操作として扱う。新規画像は使わない。

### 34.1 配置

- 左に CSS の山折りマーク、`CLIP FOLD`、状態（`READY` または `COOL 0:12`）。
- 右に `圧縮` ボタン。下にコンデンサより細い冷却バー。
- 未解放時は帯全体を描画しない。

### 34.2 状態

- 準備完了はシアンの輪郭ボタンと `READY`。
- 冷却中はバーが残り時間に応じて減り、ボタンを無効化する。点滅は使わない。
- 色だけでなく `READY` / `COOL` の文字で判別する。

### 34.3 完了条件

1. 累計200クリップ以降に折畳圧縮帯が見える
2. 準備完了と冷却が文字で分かる
3. 圧縮ボタンが44px以上でキーボード操作できる
4. コンデンサ帯と重ならない
5. 新規画像を追加しない

## 35. 二十九巡目：線径選別

折畳圧縮帯の直下へ、同じ横幅の線径選別帯を置く。3つの線径ボタンを並べ、正しいボタンを色ではなく太さのマークと文字で示す。新規画像は使わない。

### 35.1 配置

- 左に `GAUGE SORT` と状態（`TARGET 並` または `COOL 0:07`）。
- 右に `細` `並` `太` の3ボタン。各ボタンはCSSの線の太さマーク＋文字。高さ44px以上。
- PCでも狭い画面でも3ボタンは横並び、幅は均等。
- 未解放時は帯全体を描画しない。

### 35.2 状態

- 選択可能はシアン輪郭。正しいボタンを色で光らせない。
- 冷却中は3ボタンとも無効。点滅は使わない。
- 色だけでなく `TARGET` / `COOL` と線径名で判別する。

### 35.3 完了条件

1. 累計150クリップ以降に線径選別帯が見える
2. 目標線径が文字で読める
3. 3ボタンがキーボードで操作できる
4. 折畳圧縮帯と重ならない
5. 新規画像を追加しない


