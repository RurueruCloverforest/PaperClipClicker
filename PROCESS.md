# 作業方針

## フェーズ
現在はPoCのフェーズなのでどのエージェントも要求、画像生成、実装を進めても大丈夫です。
破壊的変更を許可します。バランスよりボリュームを稼ぐことを目標とします。

## ワークログ

- 作業内容は `WorkLog/` 配下にワークログとして残します。
- ワークログには、実施した内容、主な判断、変更したファイル、確認結果を簡潔に記載します。
- ファイル名は `YYYY-MM-DD_作業名.md` を基本とします。
- 同じ作業を複数日にわたって行う場合は、既存ログへの追記または日付ごとの分割を状況に応じて選びます。

## やりかけの作業

- 作業を完了できず後で続ける必要がある場合は、`WorkingTask/` 配下に状況を記載します。
- 記録には、目的、完了済みの内容、残っている内容、次に行うこと、注意点を含めます。
- 作業を再開するときは、該当する記録を最初に確認します。
- 作業が完了したら、該当する `WorkingTask/` の記録を削除します。
- 完了した内容は削除前に `WorkLog/` のワークログへ反映します。

