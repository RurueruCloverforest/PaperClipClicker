# 改善ループ4：アートワーク作成者

## 実施内容

- 生産パネルへ異常クリップ用の操作ボタンを追加した。
- 既存の `paperclip_main` を素材台帳経由で再利用した。
- 走査リング、残り時間、ゲージ、緊急表示、モバイル配置、低モーション対応をCSSで用意した。
- 統計へ異常回収数の表示枠を追加した。

## 主な判断

- PCでは絶対配置、モバイルでは予約領域へ切り替えて、主要表示との重なりとレイアウト移動を防ぐ。
- 素材を複製せず `src/artwork.ts` の参照を維持した。

## 変更ファイル

- `src/ui/render.ts`
- `src/style.css`
- `ROLE.md`

## 引き継ぎ

実装者は出現タイマー、期限切れ、2種類の報酬、保存、回収操作を接続する。
