# ペーパークリップクリッカー - 技術設計方針

## コンセプト
AIのペーパークリップ問題（クリップ最大化AIの思考実験）を題材にした、初代 Cookie Clicker 的なブラウザ放置クリッカーゲーム。手軽にブラウザで遊べることを最優先する。

## 技術スタック

| 領域 | 選定 | 備考 |
|---|---|---|
| 言語 | TypeScript | 型注釈のコストはAI（Claude Code）が肩代わりするため、学習・記述コストのデメリットを実質無視できる |
| ビルドツール | Vite | 開発サーバー・HMR・本番ビルド（静的ファイル出力）を最小構成で提供 |
| UIフレームワーク | なし（Vanilla DOM操作） | クリッカーゲームの規模ではReact等のオーバーヘッドが不要。状態→DOM反映はシンプルな自前レンダラで十分 |
| スタイリング | 素のCSS（CSS変数 + Grid/Flexbox） | `prefers-color-scheme`でダークモード対応、トランジションで近代的な質感を出す |
| 状態の永続化 | `localStorage` | セーブ/ロード、オフライン進行計算に使用 |
| デプロイ | 静的ホスティング（GitHub Pages想定） | `vite build`の出力(`dist/`)をそのまま配置するだけ |

## 試用ビルド

- 通常の `index.html` はVite開発サーバーから開くためのエントリポイントとする。
- 通常は `trial/index.html` を直接開いて試用できるよう、CSS、JavaScript、使用画像を単一HTMLへ埋め込む。
- 直接起動時の保存可否は、各ブラウザの `file://` に対する `localStorage` の扱いに依存する。
- 直接起動を制限するブラウザでは、`trial/START_TRIAL.cmd` を実行してローカルHTTPサーバーから開く。
- `npm run build:trial` で試用用ファイルを生成し、最後に `trial/index.html` を単一ファイル化する。
- ソースを変更した場合は、通常の本番ビルドと試用ビルドの両方を更新する。

## 却下した選択肢と理由
- **完全な素のJS（ビルドなしHTML直開き）**: 最速で動かせるが、コード規模が育つと保守しづらい。今回はAIが書く前提でTSの型コストが低いため採用見送り。
- **JSDoc + `tsc --noEmit`（ビルドレスTS型チェック）**: ビルド不要という利点はあるが、Viteの開発体験（HMR等）を捨てるほどのメリットではないと判断。
- **React/Vue等のUIフレームワーク**: 状態が「数値カウンタ＋アップグレード一覧」中心で、仮想DOMの恩恵が薄い。バンドルサイズと学習コストを避ける。

## ディレクトリ構成（予定）
```
NewPaperClicker/
├── index.html
├── src/
│   ├── main.ts          # エントリポイント
│   ├── state.ts          # ゲーム状態の型定義・初期値
│   ├── save.ts            # localStorage セーブ/ロード、オフライン進行計算
│   ├── game/
│   │   ├── clips.ts        # クリップ生産ロジック（クリック、自動生産）
│   │   ├── upgrades.ts     # アップグレード定義・購入処理
│   │   └── loop.ts          # メインゲームループ（tick処理）
│   ├── ui/
│   │   ├── render.ts        # 状態→DOM描画
│   │   └── components/      # ボタン、パネル等の描画関数群
│   └── style.css
├── public/                # 静的アセット（アイコン等）
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 今後の拡張余地
- 実績システム、研究ツリー、複数リソース（クリップ→ワイヤー→資金など）が増えても、`game/`配下にモジュール追加していく形で対応可能。
- 将来的にUIが複雑化した場合のみ、軽量UIライブラリ（Preact等）導入を再検討。
