# アートワーク管理方針

## 目的

画像などの素材は、出所と内容を人間およびLLMが確認しやすい形で保管し、ゲーム内では用途に適した軽量なデータへ加工して使用します。

## 素材の保管場所

- 元の素材はプロジェクト直下の `Material/` に保管します。
- `Material/` の中は、1素材につき1フォルダを作成します。
- フォルダ名は素材を識別できる英数字の名前にします。
- 異なる素材を同じフォルダへまとめません。
- 同じ素材の元画像、説明、権利情報は同じフォルダで管理します。

```text
Material/
├── paperclip_icon/
│   ├── original.png
│   └── MATERIAL.md
└── factory_background/
    ├── original.psd
    ├── original.png
    └── MATERIAL.md
```

## 素材フォルダに必ず入れるもの

各素材フォルダには、次の2種類のファイルを必ず入れます。

1. 加工前の元素材
2. 素材の内容を説明する `MATERIAL.md`

未生成の素材については、元素材の代わりに後述する `PLEASE_GENERATE_ME.md` を置きます。

`MATERIAL.md` は、LLMが画像を毎回解析しなくても用途と内容を理解できるよう、具体的に記述します。

さらに、ファイル形式が対応している場合は、元素材の画像自体にも同じ内容の要約を説明メタデータとして埋め込みます。画像ファイル単体で移動した場合にも、何の素材かを判別できる状態を目指します。

```md
# 素材名

- 種類：アイコン、背景、イラストなど
- 内容：画像に何が描かれているか
- 想定用途：ゲーム内のどこで使う素材か
- 元ファイル：`original.png`
- サイズ：1024 × 1024 px
- 背景：透明または不透明
- 出所：自作、生成AI、購入、配布サイトなど
- 作者・生成方法：分かる範囲で記載
- ライセンス：利用条件、クレジット表記の要否
- 作成・取得日：YYYY-MM-DD
- 備考：色、構図、加工時の注意など
```

出所やライセンスが不明な素材は、確認が終わるまでゲームに使用しません。

## 未生成素材の依頼方法

画像を生成できないエージェントが素材を必要とする場合は、次の手順で生成依頼を残します。

1. `Material/` 配下に、対象素材専用のフォルダを作成します。
2. フォルダ内の `MATERIAL.md` に、必要な素材の内容と想定用途を記載します。
3. 同じフォルダに `PLEASE_GENERATE_ME.md` を作成し、素材が未生成であることを示します。

```text
Material/
└── paperclip_icon/
    ├── MATERIAL.md
    └── PLEASE_GENERATE_ME.md
```

- `MATERIAL.md` は画像が未生成でも、種類、描画内容、想定用途、背景、構図、雰囲気など、分かる範囲を具体的に記載します。
- 出力ファイル名、画像サイズ、縦横比、形式、透過の要否、色、避けたい表現などに指定がある場合は、`PLEASE_GENERATE_ME.md` に記載します。
- 特別な指定がない場合も、`PLEASE_GENERATE_ME.md` は未生成の目印として空にせず、依頼の概要を記載します。
- 画像生成が可能なエージェントは、`PLEASE_GENERATE_ME.md` がある素材フォルダを未生成素材として扱います。
- 生成時は `MATERIAL.md` と `PLEASE_GENERATE_ME.md` の両方を確認します。
- 生成した元素材を同じ素材フォルダへ保存し、実際のファイル名、サイズ、生成方法などを `MATERIAL.md` に追記します。
- 生成と記録が完了したら `PLEASE_GENERATE_ME.md` を削除します。マーカーファイルを残したまま完了扱いにはしません。

`PLEASE_GENERATE_ME.md` の記載例は次のとおりです。

```md
# 画像生成依頼

- 出力ファイル名：`original.png`
- サイズ：1024 × 1024 px
- 形式：PNG
- 背景：透明
- 必須条件：中央にペーパークリップを1つ配置
- 避ける表現：文字、ロゴ、複雑な背景
- 備考：詳しい内容と用途は `MATERIAL.md` を参照
```

## 画像メタデータ

- 可能な場合は、元素材の画像にタイトル、説明、キーワード、作者、著作権、出所などのメタデータを埋め込みます。
- 説明には、画像に何が描かれているかと想定用途を簡潔に記載します。
- JPEGではEXIF、IPTC、XMP、PNGではテキスト情報、WebPではEXIFやXMPなど、形式が対応する標準的な領域を使用します。
- メタデータの追加によって画像の表示、色、透過、画質を変化させないようにします。
- 出所やライセンス表記を埋め込む場合は、元の情報を省略または改変しません。
- 編集ソフトや変換処理によってメタデータが削除される可能性があるため、メタデータだけを唯一の記録にはしません。
- `MATERIAL.md` を人間とLLMが参照する正式な説明として維持し、画像メタデータはその補助として扱います。
- ゲーム用に軽量化した画像は、容量削減のため不要なメタデータを削除して構いません。ただし、対応する元素材と `MATERIAL.md` は必ず残します。

## 元素材の扱い

- 元素材は再加工できる品質と解像度を保ちます。
- 元素材を直接上書きしません。
- トリミング、リサイズ、圧縮、色調整などは複製したデータに対して行います。
- レイヤーを含む編集用ファイルがある場合は、書き出し画像と一緒に保存します。
- 元素材のファイル名は `original` を基本とし、複数ある場合は内容が分かる名前を付けます。

## ゲームで使用する画像

- ゲームから `Material/` 内の元素材を直接参照しません。
- ゲーム内の画像パスを各UIへ直接記述せず、`src/artwork.ts` の素材台帳を経由して参照します。
- 素材台帳には素材ID、公開用パス、対応する `Material/` フォルダ、寸法、代替テキストを記録します。
- 公開用パスはViteのベースURLを使って組み立て、Webデプロイ版と `trial/` 版のどちらでも同じコードから解決できるようにします。
- 元素材を必要な表示寸法にリサイズし、用途に適した形式と品質へ変換します。
- 不要な余白、メタデータ、過剰な解像度を取り除きます。
- 写真や複雑な背景にはWebPなど、透過や輪郭が重要な素材にはWebPまたはPNGなど、用途に合う形式を選びます。
- 小さな表示に巨大な画像を読み込ませないようにします。
- 見た目を損なわない範囲で圧縮し、ダウンロード容量と描画負荷を抑えます。
- ゲーム用に加工した画像は `public/assets/` など、実装側で定めた公開用フォルダへ配置します。
- ゲーム用画像のファイル名は、用途と内容が分かる名前にします。

## 代替テキスト

- 情報を伝える画像には、その画面上での意味が分かる簡潔な代替テキストを設定します。
- 隣接する見出しやボタン名と同じ意味を繰り返す装飾画像には `alt=""` を設定し、必要に応じて `aria-hidden="true"` を付けます。
- 情報を伝える画像をCSSの背景画像として使用しません。
- 同じ素材でも使用状況によって適切な代替テキストが異なるため、素材台帳に単独表示用と装飾用の値を区別して記録します。
- `MATERIAL.md` に、実際の使用箇所とそこで採用した代替テキストの方針を記載します。

```text
Material/paperclip_icon/original.png
        ↓ リサイズ・圧縮
public/assets/icons/paperclip.webp
```

## 更新時のルール

- ゲーム用画像を作り直す場合も、元素材から再生成します。
- 元素材を変更した場合は、必要に応じて `MATERIAL.md` とゲーム用画像を更新します。
- ゲーム用画像だけでは編集や再生成の根拠が失われるため、対応する元素材を削除しません。
- 素材をゲームで使用しなくなっても、削除の判断は元素材と権利情報を確認してから行います。
