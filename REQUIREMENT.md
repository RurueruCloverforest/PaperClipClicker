# ペーパークリップクリッカー 初期バージョン要件定義

## 1. 目的

AIのペーパークリップ最大化問題を題材にした、ブラウザで手軽に遊べる放置型クリッカーゲームを提供する。

初期バージョンでは、プレイヤーが手動でクリップを作り、自動生産設備を購入し、生産速度が増えていく基本的な成長ループを完成させることを目的とする。

## 2. 対象環境

- PCおよびスマートフォンのモダンブラウザで動作すること
- Chrome、Edge、Firefox、Safariの現行メジャーバージョンを対象とすること
- インストールやアカウント登録を必要としないこと
- 静的Webサイトとして配信できること
- 通信が切断された後も、読み込み済みのゲームは継続して遊べること

## 3. 初期バージョンのゲーム体験

プレイヤーは「クリップを作る」ボタンを押してクリップを生産する。貯めたクリップを消費して自動生産設備やアップグレードを購入し、1クリック当たりの生産量と1秒当たりの自動生産量を増やす。

基本ループは次のとおりとする。

1. ボタンを押してクリップを作る
2. クリップを貯める
3. 自動生産設備やアップグレードを購入する
4. 生産効率が上がり、より多くのクリップを作れるようになる
5. 上位の設備を購入する

初期バージョンの明確な終了条件は設けず、累計生産数の更新を継続的な目標とする。

## 4. 機能要件

### 4.1 手動生産

- 「クリップを作る」ボタンを表示すること
- 初期状態では、クリックまたはタップ1回につき1クリップを生産すること
- 生産時に現在のクリップ数と累計生産数を更新すること
- ボタン操作に短い視覚的フィードバックを表示すること

### 4.2 自動生産

- 購入した設備に応じてクリップを自動生産すること
- 自動生産量を経過時間に基づいて計算し、描画頻度に結果が左右されないこと
- 画面には合計の「毎秒生産量」を表示すること

初期設備は以下とする。

| 設備 | 基本価格 | 基本生産量 | 解放条件 |
|---|---:|---:|---:|
| オートクリッパー | 15クリップ | 0.1クリップ/秒 | 最初から表示 |
| ワイヤー加工機 | 100クリップ | 1クリップ/秒 | 累計50クリップ生産 |
| クリップ工場 | 1,100クリップ | 8クリップ/秒 | 累計500クリップ生産 |
| AI生産ライン | 12,000クリップ | 47クリップ/秒 | 累計5,000クリップ生産 |

- 同じ設備を複数購入できること
- 設備価格は所有数に応じて `基本価格 × 1.15^所有数` とし、購入時は整数に切り上げること
- 未解放の設備は原則として非表示とし、解放時に通知すること

### 4.3 アップグレード

- 条件を満たしたアップグレードを一覧に表示すること
- 購入可能な場合のみ購入できること
- 各アップグレードは1回だけ購入できること
- 購入後は効果を即時反映すること

初期アップグレードは以下とする。

| 名称 | 価格 | 解放条件 | 効果 |
|---|---:|---|---|
| 精密な指先 | 100クリップ | 累計50クリップ生産 | クリック生産量を2倍 |
| 強化レバー | 500クリップ | 累計250クリップ生産 | クリック生産量を2倍 |
| 高品質カッター | 250クリップ | オートクリッパーを10台所有 | オートクリッパーの生産量を2倍 |
| 工程最適化 | 2,500クリップ | ワイヤー加工機を10台所有 | ワイヤー加工機の生産量を2倍 |
| 自己改善アルゴリズム | 25,000クリップ | AI生産ラインを1台所有 | 全設備の生産量を1.5倍 |

### 4.4 数値表示

- 現在のクリップ数を画面内で最も目立つ位置に表示すること
- クリック当たりの生産量、毎秒生産量、累計生産数を表示すること
- 内部計算では小数を保持すること
- 通常表示は小数第1位までを基本とし、不要な末尾の `.0` は省略すること
- 大きな数値は `K`、`M`、`B` などの短縮表記にすること
- 購入価格と購入可否が分かること

### 4.5 セーブとロード

- ゲーム状態をブラウザの `localStorage` に保存すること
- 10秒ごとの自動保存、ならびにページ終了時の保存を行うこと
- 次回アクセス時に自動でロードすること
- 保存対象には現在値、累計値、設備所有数、購入済みアップグレード、最終保存日時を含めること
- 保存データにバージョン番号を含め、将来の移行処理を追加できる構造にすること
- 保存データが不正または破損している場合は、ゲームを停止させず初期状態で開始すること
- 設定画面から確認操作を経てセーブデータを初期化できること

### 4.6 オフライン進行

- 再訪時に、最終保存時刻からの経過時間に応じた自動生産分を加算すること
- オフライン進行の上限は8時間とすること
- 復帰時に、不在時間と獲得クリップ数を通知すること
- 時計の巻き戻しなどで経過時間が負になる場合は、オフライン報酬を与えないこと

### 4.7 設定

- ライト、ダーク、端末設定に追従するテーマを選択できること
- 数値短縮表記のオン・オフを切り替えられること
- セーブデータ初期化機能を提供すること
- 設定内容も保存すること

## 5. 画面要件

### 5.1 メイン画面

メイン画面は以下の領域で構成する。

- ヘッダー：ゲーム名、設定ボタン
- 生産パネル：現在のクリップ数、毎秒生産量、「クリップを作る」ボタン
- 設備パネル：解放済み設備の名称、所有数、生産量、価格、購入ボタン
- アップグレードパネル：購入可能なアップグレードの名称、効果、価格
- 統計パネル：累計生産数、クリック当たりの生産量、プレイ時間
- 通知領域：設備解放、購入、オフライン報酬などの短時間通知

### 5.2 レスポンシブ表示

- 横幅の広い画面では、生産・設備・アップグレードを複数カラムで表示すること
- 狭い画面では1カラムに並べ、横スクロールを発生させないこと
- タップ対象はスマートフォンで操作しやすい大きさを確保すること
- 主要操作を画面拡大なしで行えること

### 5.3 視覚表現

- ペーパークリップ、金属、機械、AIを連想させる統一された配色と質感を用いること
- 購入可能、購入不可、未解放の状態を色だけに依存せず区別できること
- 数値更新によってレイアウトが大きく揺れないこと
- アニメーションは短く、ゲーム操作を妨げないこと
- `prefers-reduced-motion` が有効な場合は不要なアニメーションを抑制すること

## 6. 操作性・アクセシビリティ要件

- マウス、タッチ、キーボードで主要操作ができること
- Tabキーによるフォーカス移動とEnterまたはSpaceキーによるボタン操作に対応すること
- フォーカス位置を視覚的に確認できること
- 操作部品には意味の分かるラベルを付けること
- 文字と背景には十分なコントラストを持たせること
- 自動更新される数値をスクリーンリーダーが過剰に読み上げない構成にすること

## 7. 非機能要件

- 初回表示後、通常操作に対して即座に視覚的反応を返すこと
- ゲーム計算と画面描画を分離すること
- 画面全体を毎フレーム再構築せず、変化した要素を中心に更新すること
- バックグラウンドタブから復帰した際も、経過時間に基づいて生産量を正しく補正すること
- JavaScriptエラーが発生しても、可能な範囲で保存済みデータを失わないこと
- 外部APIやバックエンドを必要としないこと
- 初期バージョンでは広告、トラッキング、外部分析サービスを組み込まないこと

## 8. 技術要件

- TypeScript、Vite、Vanilla DOM API、CSSを使用すること
- 本番ビルドではHTML、JavaScript、CSSおよび静的アセットを `dist/` に出力すること
- `npm run dev` で開発サーバーを起動できること
- `npm run build` で型チェックと本番ビルドを実行できること
- GitHub Pagesなどの静的ホスティングで配信可能な相対パス設定にすること
- ゲームロジックはDOMに依存させず、単体で検証できる構造にすること

## 9. 初期バージョンに含めないもの

以下は将来拡張候補とし、初期バージョンには含めない。

- ユーザーアカウント、クラウドセーブ、端末間同期
- オンラインランキング、対戦、協力プレイ
- 課金、広告
- 効果音、BGM
- 実績システム
- プレステージまたは周回システム
- 研究ツリー
- ワイヤー、資金、電力などの複数リソース
- ストーリーイベントやエンディング
- 多言語切り替え
- PWAおよびオフライン初回起動

## 10. 受け入れ条件

初期バージョンは、以下をすべて満たしたとき完成とみなす。

1. 新規プレイヤーが手動操作でクリップを生産できる
2. 4種類の設備を解放、購入し、自動生産を増やせる
3. 5種類のアップグレードを解放、購入し、効果が正しく反映される
4. 所持数不足時に購入できず、購入時に正しい価格が差し引かれる
5. ページを再読み込みしてもゲーム状態と設定が復元される
6. 8時間を上限とするオフライン進行が正しく加算される
7. セーブデータを確認操作付きで初期化できる
8. PCとスマートフォン相当の画面幅で主要機能を利用できる
9. キーボードだけで主要操作を実行できる
10. 型チェックおよび本番ビルドがエラーなく完了する

## 11. 将来検討事項

- ゲームバランスは実プレイ後に調整する
- 初期版のプレイ時間と離脱点を確認し、設備・アップグレードの追加を判断する
- 複数リソースや研究ツリーを導入する場合は、初期版の単純な成長ループを損なわないよう段階的に解放する
- セーブ形式を変更する場合は、既存データの移行方針を定める

## 12. PoC拡張サイクル1

初期の自動化以降も規模が高速に拡大する体験を確認するため、以下を次の実装対象とする。

### 12.1 生産設備の追加

既存4設備に続き、以下の設備を追加する。

| 設備 | 基本価格 | 基本生産量 | 解放条件 |
|---|---:|---:|---:|
| ナノフォージ | 130,000クリップ | 260クリップ/秒 | 累計50,000クリップ生産 |
| スウォーム組立群 | 1,400,000クリップ | 1,400クリップ/秒 | 累計500,000クリップ生産 |
| 軌道上クリップ工廠 | 15,000,000クリップ | 7,800クリップ/秒 | 累計5,000,000クリップ生産 |
| 物質コンパイラ | 160,000,000クリップ | 44,000クリップ/秒 | 累計50,000,000クリップ生産 |

- 追加設備にも既存と同じ価格上昇式を適用する。
- 新設備の解放間隔は、直前の設備を購入してから数分程度で到達可能な速度を目安とする。
- 設備カードには1台当たりだけでなく、その設備全体の毎秒生産量を表示する。

### 12.2 アップグレードの追加

| 名称 | 価格 | 解放条件 | 効果 |
|---|---:|---|---|
| 並列アクチュエータ | 75,000クリップ | AI生産ラインを10台所有 | AI生産ラインの生産量を2倍 |
| ナノ精度管理 | 600,000クリップ | ナノフォージを10台所有 | ナノフォージの生産量を2倍 |
| 群知能同期 | 6,000,000クリップ | スウォーム組立群を10群所有 | スウォーム組立群の生産量を2倍 |
| 軌道物流最適化 | 60,000,000クリップ | 軌道上クリップ工廠を5基所有 | 軌道上クリップ工廠の生産量を2倍 |
| 再帰的命令 | 600,000,000クリップ | 物質コンパイラを1基所有 | 全設備の生産量を2倍 |

### 12.3 まとめ買い

- 設備の購入数量を `×1`、`×10`、`MAX` から選択できること。
- `×10` は次の10台分の価格合計を表示し、全額を所持している場合だけ購入できること。
- `MAX` は現在所持しているクリップで購入できる最大数を一度に購入すること。
- `MAX` で1台も購入できない場合は購入ボタンを無効化すること。
- 選択中の購入数量は保存対象とすること。

### 12.4 次の目標

- メイン生産パネルに、次に解放される設備名と必要累計生産数を表示すること。
- 現在の累計生産数から解放までの進行率をプログレスバーで表示すること。
- すべての設備を解放した後は、次の目標として累計生産数の桁更新を表示すること。

### 12.5 生産フェーズ

現在の累計生産数に応じ、以下の生産フェーズ名を表示する。

| 累計生産数 | フェーズ名 |
|---:|---|
| 0 | 手動プロトコル |
| 50 | 機械化 |
| 5,000 | AI自律生産 |
| 50,000 | ナノスケール製造 |
| 5,000,000 | 軌道工業化 |
| 50,000,000 | 物質変換 |

- フェーズ切り替え時には通知すること。
- 現在のフェーズは保存データから累計生産数を基に再計算できるものとする。

### 12.6 システムログ

- 設備解放、設備購入、アップグレード導入、フェーズ移行を短いシステムログとして画面に残すこと。
- 最新6件を表示し、古い項目から取り除くこと。
- ログは演出情報とし、セーブ対象には含めなくてよい。

### 12.7 追加受け入れ条件

1. 合計8種類の設備が条件に従って段階的に解放される
2. 合計10種類のアップグレードが購入でき、効果が正しく反映される
3. `×1`、`×10`、`MAX` の設備購入価格と購入数が正しい
4. 選択した購入数量が再読み込み後も復元される
5. 次の設備解放までの目標と進行率が表示される
6. 累計生産数に応じてフェーズ表示が切り替わる
7. 主要イベントが最新6件のシステムログへ表示される
8. Webデプロイ版と試用版の両方で追加機能が動作する

## 13. PoC拡張サイクル2：設備マイルストーン

設備を買い集める行為そのものに短期目標と報酬を与えるため、設備所有数に応じた恒久的な生産倍率を追加する。このサイクルでは新しい独立システムを設備マイルストーン1つに限定する。

### 13.1 マイルストーン条件と効果

すべての設備に共通して、以下の所有数へ到達した時点で、その設備の生産倍率を上昇させる。

| 所有数 | 到達後の設備倍率 | 到達時の表示名 |
|---:|---:|---|
| 10 | 2倍 | 安定稼働 |
| 25 | 4倍 | 並列生産 |
| 50 | 8倍 | 大規模展開 |
| 100 | 16倍 | 完全最適化 |

- 倍率は段階ごとの追加乗算ではなく、表に示した到達後の最終倍率とする。
- マイルストーン倍率は設備固有アップグレードおよび全体倍率と乗算する。
- 到達判定は設備所有数から計算し、独立した解除フラグをセーブする必要はない。
- `×10` または `MAX` で複数の条件を一度に越えた場合は、越えたすべてのマイルストーンを達成済みとする。
- 一度に複数達成した場合、トースト通知は設備ごとに最新の到達段階1件へまとめてよい。

### 13.2 設備カード表示

- 各設備カードに現在のマイルストーン倍率を表示すること。
- 次のマイルストーンまでの所有数を `現在数 / 目標数` で表示すること。
- 次のマイルストーンまでの進行を細いプログレスバーで表示すること。
- 所有数100以上では「完全最適化」と16倍を表示し、プログレスバーを完了状態にすること。
- マイルストーン情報は購入価格や生産量より視覚的に強くしすぎないこと。

### 13.3 達成フィードバック

- マイルストーン到達時に設備名、段階名、新しい倍率を通知すること。
- システムログへ `MILE` 種別で記録すること。
- まとめ買い直後に、生産量表示へ新しい倍率を反映すること。
- 読み込み時に過去のマイルストーン通知を繰り返さないこと。

### 13.4 統計

- 統計パネルに達成済みマイルストーン数を表示すること。
- 全12設備、各4段階の合計48件を最大数とする。
- 表示形式は `達成数 / 48` とする。

### 13.5 追加受け入れ条件

1. 各設備が所有数10、25、50、100で2倍、4倍、8倍、16倍になる
2. マイルストーン倍率が既存アップグレード倍率と正しく乗算される
3. `×10` と `MAX` で条件を越えた場合も正しく達成される
4. 各設備カードに現在倍率、次の目標、進行率が表示される
5. 到達時に通知と `MILE` システムログが追加される
6. 統計に達成数が `達成数 / 48` で表示される
7. 既存セーブを読み込んだ場合、所有数から倍率が復元される
8. Webデプロイ版と試用版の両方で動作する

## 14. PoC拡張サイクル3：実績

生産規模、設備投資、アップグレード収集に長期目標を与えるため、既存のゲーム状態から自動解除される実績システムを追加する。このサイクルでは新しい独立システムを実績1つに限定する。

### 14.1 初期実績

| ID | 名称 | 条件 |
|---|---|---|
| `firstClip` | 最初の一本 | 累計1クリップ生産 |
| `hundredClips` | 机いっぱい | 累計100クリップ生産 |
| `tenThousandClips` | 小さな産業 | 累計10,000クリップ生産 |
| `millionClips` | 百万の命令 | 累計1,000,000クリップ生産 |
| `hundredMillionClips` | 地平線を覆う | 累計100,000,000クリップ生産 |
| `billionClips` | 十億の回答 | 累計1,000,000,000クリップ生産 |
| `firstMachine` | 自動化の始まり | 設備を合計1台所有 |
| `tenMachines` | 機械室 | 設備を合計10台所有 |
| `fiftyMachines` | 生産区域 | 設備を合計50台所有 |
| `hundredMachines` | 止まらない工場 | 設備を合計100台所有 |
| `firstUpgrade` | 改善の余地 | アップグレードを1個導入 |
| `fiveUpgrades` | 最適化傾向 | アップグレードを5個導入 |
| `allUpgrades` | 自己改善完了 | アップグレードを16個導入 |
| `fullSpectrum` | 全領域生産 | 12種類すべての設備を1台以上所有 |

### 14.2 実績ボーナス

- 解除済み実績1個につき、全設備の生産量を2%増加させる。
- 実績ボーナスは加算し、全体倍率を `1 + 解除数 × 0.02` とする。
- 14個すべて解除した場合の実績倍率は1.28倍とする。
- 実績倍率は設備マイルストーン、設備固有アップグレード、全体アップグレードと乗算する。
- クリック生産量には適用しない。
- 解除状態は現在のゲーム状態から導出し、独立した解除フラグをセーブしない。

### 14.3 実績パネル

- メイン画面下部に実績パネルを表示する。
- 全14件を、解除済みと未解除の両方を含めて表示する。
- 各実績に名称、解除条件、状態を表示する。
- 解除済みはアイコンと名称を明瞭にし、未解除は低コントラストと鍵記号で区別する。
- 実績一覧はPCでは複数カラム、スマートフォンでは1または2カラムにする。
- 現在の実績倍率と解除数をパネル見出し付近に表示する。

### 14.4 解除フィードバック

- 新しい実績を解除したとき、名称と全設備ボーナスの増加をトーストで通知する。
- システムログへ `ACHV` 種別で記録する。
- 同時に複数解除した場合は、それぞれを解除済みにする。通知は最大3件まで個別表示し、それ以上はまとめてもよい。
- 保存データ読み込み時には、過去に条件を満たした実績を再通知しないこと。

### 14.5 統計

- 統計パネルに実績解除数を `解除数 / 14` で表示する。
- 実績パネルには全設備へ適用中の実績倍率を表示する。

### 14.6 追加受け入れ条件

1. 14個の実績が定義された条件に従って解除される
2. 実績1個につき全設備の生産量が2%ずつ増える
3. 実績倍率が既存アップグレードおよびマイルストーン倍率と正しく乗算される
4. 解除済みと未解除の実績が視覚的かつテキストで区別できる
5. 新規解除時に通知と `ACHV` システムログが追加される
6. 統計と実績パネルに解除数が表示される
7. 読み込み時に過去の実績通知を繰り返さない
8. 既存セーブから現在条件に合う実績が復元される
9. Webデプロイ版と試用版の両方で動作する

## 13. PoC拡張サイクル2

サイクル1で確立した「規模がひたすら拡大し続ける楽しさ」を維持しつつ、AIのペーパークリップ最大化問題が持つ「工場から惑星、恒星、銀河、そして観測可能な宇宙全体へ」という古典的な拡大の連想に沿って、次の実装対象を定義する。

### 13.1 生産設備の追加

既存8設備に続き、以下の設備を追加する。基本価格の対前設備比は既存設備群とほぼ同じ約10.7倍を維持する。

| 設備 | 基本価格 | 基本生産量 | 解放条件 |
|---|---:|---:|---:|
| 惑星改造アセンブラ | 1,800,000,000クリップ | 250,000クリップ/秒 | 累計500,000,000クリップ生産 |
| 恒星光電子収穫網 | 19,000,000,000クリップ | 1,400,000クリップ/秒 | 累計5,000,000,000クリップ生産 |
| 銀河複製艦隊 | 200,000,000,000クリップ | 7,800,000クリップ/秒 | 累計50,000,000,000クリップ生産 |
| 因果地平最適化機関 | 2,100,000,000,000クリップ | 44,000,000クリップ/秒 | 累計500,000,000,000クリップ生産 |

- 各設備の説明文は、惑星の地殻、恒星のエネルギー、銀河への自己複製拡散、観測可能な範囲全体の最適化、という連想が伝わる短い一文とする。
- 追加設備にも既存と同じ価格上昇式（`基本価格 × 1.15^所有数`）を適用する。
- 因果地平最適化機関は、この段階で到達しうる最終設備として扱う。

### 13.2 アップグレードの追加

| 名称 | 価格 | 解放条件 | 効果 |
|---|---:|---|---|
| 地殻精製最適化 | 7,000,000,000クリップ | 惑星改造アセンブラを10基所有 | 惑星改造アセンブラの生産量を2倍 |
| 恒星同調制御 | 75,000,000,000クリップ | 恒星光電子収穫網を10基所有 | 恒星光電子収穫網の生産量を2倍 |
| 自己複製最適化 | 800,000,000,000クリップ | 銀河複製艦隊を10隻所有 | 銀河複製艦隊の生産量を2倍 |
| 因果収束命令 | 8,000,000,000,000クリップ | 因果地平最適化機関を1基所有 | 全設備の生産量を2倍 |

### 13.3 クリティカル生産

- 「クリップを作る」ボタンのクリックごとに、基本5%の確率でクリティカルが発生すること。
- クリティカル発生時は、通常のクリック生産量の5倍を生産すること。
- クリティカル発生時は、通常のクリックと視覚的に区別できるフィードバック（色や強調表示の違い）を表示すること。
- クリティカルの発生頻度が高いため、システムログへは記録しないこと。フィードバックは浮動表示のみで足りるものとする。
- クリティカル関連の効果を持つ、以下2種類のアップグレードを追加する。

| 名称 | 価格 | 解放条件 | 効果 |
|---|---:|---|---|
| 過負荷プロトコル | 2,000クリップ | 累計1,000クリップ生産 | クリティカル発生率を5%上昇（合計10%） |
| 共鳴増幅 | 3,000,000クリップ | 累計1,000,000クリップ生産 | クリティカル倍率を5倍から8倍へ上昇 |

### 13.4 生産フェーズの追加

既存のフェーズに続き、以下を追加する。

| 累計生産数 | フェーズ名 |
|---:|---|
| 5,000,000,000 | 恒星規模採取 |
| 500,000,000,000 | 因果地平到達 |

- 「因果地平到達」は、観測可能な範囲のすべてが最適化の対象になったことを示す、この段階での最終フェーズとして扱う。
- フェーズ切り替え時の通知方法は既存フェーズと同じ方式に従う。

### 13.5 追加受け入れ条件

1. 合計12種類の設備が条件に従って段階的に解放される
2. 合計16種類のアップグレードが購入でき、効果が正しく反映される
3. クリック時に基本5%の確率でクリティカルが発生し、通常の5倍のクリップを生産する
4. クリティカル関連の2種類のアップグレードにより、発生率または倍率が変化する
5. 合計8種類の生産フェーズが累計生産数に応じて切り替わる
6. Webデプロイ版と試用版の両方で追加機能が動作する
## 15. PoC拡張サイクル4：異常クリップ

放置生産を眺めている時間に短い判断と報酬を加えるため、画面上へ一時的に現れるランダムボーナス「異常クリップ」を追加する。このサイクルでは新しい独立システムを異常クリップ1つに限定する。

### 15.1 出現

- ゲーム開始または再読み込みから20〜40秒後に最初の異常クリップが出現する。
- 2回目以降は、直前の回収または消滅から30〜75秒後に出現する。
- 出現中は新しい異常クリップを重ねて出現させない。
- 出現から12秒で未回収のまま消滅する。
- バックグラウンド中に期限を過ぎた場合は報酬を与えず、復帰後に次回出現を予約する。
- 出現状態と次回時刻はセーブせず、オフライン報酬にも含めない。

### 15.2 回収報酬

期限内に異常クリップを押すと、以下から等確率で1種類を即時獲得する。

| 報酬 | 獲得量 |
|---|---:|
| 生産キャッシュ | `max(100, 現在の毎秒生産量 × 60)` |
| 在庫エコー | `max(100, 現在所持クリップ × 10%)` |

- 報酬は現在所持数と累計生産数の両方へ加算する。
- 回収後は即座に消え、次回出現を予約する。
- 統計に累計回収数を保存・表示する。
- 回収時は報酬名と獲得量を通知し、システムログへ `BONUS` として記録する。

### 15.3 表示と操作

- 異常クリップは生産パネル内に表示し、ページ幅やスクロール位置によって画面外へ出ないこと。
- 残り秒数と時間経過を文字およびゲージで示し、色だけに依存しないこと。
- マウス、タッチ、キーボードで回収でき、意味の分かるラベルを持つこと。
- 出現・消滅によって主要レイアウトを移動させないこと。
- `prefers-reduced-motion` が有効な場合は浮遊や点滅を抑制すること。

### 15.4 追加受け入れ条件

1. 指定範囲内のランダムな時間で異常クリップが1つだけ出現する
2. 12秒以内に回収しない場合は報酬なしで消滅する
3. 2種類の報酬計算が最低100クリップを保証し、現在値と累計値へ正しく加算される
4. 回収数が保存・復元され、統計へ表示される
5. PCとスマートフォン幅で表示と操作ができる
6. Webデプロイ版と試用版の両方で動作する

## 16. PoC拡張サイクル5：自動購入コア

規模が拡大するほど手動での設備購入操作が繰り返し作業になっていくため、プレイヤーが個別に解放・設定できる範囲内で設備購入を自動化する「自動購入コア」を追加する。このサイクルでは新しい独立システムを自動購入コア1つに限定する。

一般的なアイドルゲームの自動化は、プレイヤーが対象を選んで有効化する仕組みであり、無条件の全自動プレイではないことが多い。本サイクルもこの方針に従い、プレイヤーが設備ごとに自動購入の対象を選ぶ形にする。

### 16.1 解放

- 自動購入コアは、設備・アップグレードと同様に一覧から購入する一度限りのアップグレードとして扱う。
- 解放条件は累計5,000,000クリップ生産、価格は1,000,000クリップとする。
- 購入すると、以後すべての設備カードに自動購入トグルが表示される。
- 自動購入コア自体は他のアップグレードと同じ一覧に表示し、専用パネルは設けない。

### 16.2 対象と操作

- 自動購入コア購入後、各設備カードに「自動」トグルを1つ追加する。
- トグルの初期状態はすべてOFFとする。
- トグルはクリック・タップ・キーボードで切り替えられ、`aria-pressed` で状態を伝える。
- アップグレードは自動購入の対象にせず、購入判断は引き続きプレイヤーが行う。
- 自動購入コア購入後にのみ、設備一覧の見出し付近へ「すべて自動化」「すべて解除」の一括切り替えボタンを表示する。

### 16.3 動作

- 自動購入は1秒ごとのゲームループ処理内で判定する。
- 判定対象は自動購入がONの設備のうち、現在購入可能なものとする。
- 対象が複数ある場合は価格の安いものから順に走査し、各設備につき1tickあたり1台までを購入する。
- 自動購入は選択中のまとめ買い数量（`×1`、`×10`、`MAX`）の影響を受けず、常に1台単位で購入する。
- 自動購入によるクリップ消費は手動購入と同じ計算式を使い、価格や所有数の扱いを変えない。
- 自動購入によって設備マイルストーンや解放条件を満たした場合も、既存の通知・ログの仕組みをそのまま適用する。

### 16.4 表示とフィードバック

- 自動購入コア解放時に、購入可能になったことを通知し、システムログへ `AUTO` 種別で記録する。
- 個々の自動購入自体は頻度が高いため、購入のたびの通知やシステムログ記録は行わない。
- 統計パネルに自動購入がONの設備数を `○ / 12` で表示する。
- 自動購入トグルは購入ボタンや価格表示より視覚的に控えめにし、既存の情報階層を崩さない。

### 16.5 保存

- 自動購入コアの解放状態を保存対象に含める。
- 設備ごとの自動購入ON/OFF状態を保存対象に含める。
- 保存データに自動購入コアの解放情報がない場合は、未解放かつ全設備OFFとして扱う。

### 16.6 追加受け入れ条件

1. 累計5,000,000クリップ生産後、自動購入コアを1,000,000クリップで購入できる
2. 購入後、各設備カードに自動購入トグルが表示され、ON/OFFを切り替えられる
3. ONの設備は購入可能になり次第、1秒ごとに1台ずつ自動購入される
4. アップグレードは自動購入の対象にならず、手動購入のみで導入される
5. 自動購入は選択中のまとめ買い数量に関わらず常に1台ずつ購入する
6. 自動購入コアの解放状態と設備ごとのON/OFF状態が保存・復元される
7. 統計に自動化ON設備数が `○ / 12` で表示される
8. 自動購入コア解放時に通知と `AUTO` システムログが記録される
9. Webデプロイ版と試用版の両方で動作する

## 17. PoC拡張サイクル6：世界観測

規模の拡大が数値だけでなく画面上の世界の変化としても伝わるようにする。生産フェーズに対応した観測画像をメイン画面へ表示し、設備カードの記号を実画像アイコンへ置き換える。このサイクルでは新しい独立システムを世界観測1つに限定する。設備アイコンは、観測している世界の構成要素として同時に導入する。

一般的な放置クリッカーでは、数字の増加だけでなく工場や世界の見た目が変わることで成長を実感させることが多い。本サイクルもこの方針に従い、既存8フェーズを「研究室の机から観測可能な宇宙へ侵食していく」一連の景観として見せる。

### 17.1 観測窓

- メイン生産パネル内に、現在の生産フェーズを示す観測窓を1つ表示する。
- 観測窓にはフェーズ専用の景観画像、フェーズ名、短い観測メモを含める。
- 累計生産数に応じて現在フェーズが変わったとき、観測画像と観測メモを切り替える。
- 観測メモは世界の変化を短い一文で示す演出情報とし、操作手順や思想の説明は書かない。
- 観測窓は操作対象にせず、クリックやタップで生産や購入が起きないこと。
- 出現や切り替えによって「クリップを作る」ボタンや所持数表示が押し下げられ、主要操作しづらくならないこと。

初期の観測メモは以下とする。

| フェーズ | 観測メモ |
|---|---|
| 手動プロトコル | 机上の端末が、最初の一本を待つ。 |
| 機械化 | 小さな作業室で、同じ動作が繰り返され始める。 |
| AI自律生産 | 監視する人間はいなくても、工程は改善され続ける。 |
| ナノスケール製造 | 金属は分子の単位で折られ、無駄だけが消えていく。 |
| 軌道工業化 | 地表を離れても、生産環は途切れない。 |
| 物質変換 | 世界の材料は、徐々に同じ形へ書き換えられる。 |
| 恒星規模採取 | 光そのものが、クリップの原料として計上される。 |
| 因果地平到達 | 観測できる範囲に、別の目的は残っていない。 |

### 17.2 フェーズ到達補正

新しいフェーズへ到達したとき、観測の確定に伴う一度限りの補正としてクリップを即時獲得する。

- 獲得量は `max(100, 現在の毎秒生産量 × 90)` とする。
- 報酬は現在所持数と累計生産数の両方へ加算する。
- 到達時はフェーズ名、観測メモ、獲得量を通知し、システムログへ `VIEW` 種別で記録する。
- 初期フェーズ「手動プロトコル」では補正を与えない。
- 一度に複数フェーズを越えた場合は、越えた各フェーズの補正をすべて与える。通知は最新フェーズを中心にまとめてよい。
- 補正の付与済みフェーズは保存対象とする。
- 既存セーブを読み込んだ場合、すでに到達しているフェーズへは補正を再付与しない。

### 17.3 設備アイコン

- 12種類の設備それぞれに専用のアイコン画像を用いる。
- 設備カードでは、既存の記号文字に代えてこのアイコンを表示する。
- 設備名が隣接しているため、アイコンは装飾画像として扱い、代替テキストは空でよい。
- 画像が読み込めない場合のフォールバックとして、既存の記号文字を残してよい。
- アイコンは設備の階層や世界観が伝わる実画像とし、単純な幾何記号の置き換えだけにしない。

### 17.4 表示と操作

- 観測窓はPCでは生産パネル内の横長領域、狭い画面では生産ボタンと次の目標の間に置く。
- 景観画像は情報を伝える画像とし、フェーズ名とは別に、何が描かれているかが分かる代替テキストを持つ。
- フェーズ名と観測メモは画像がなくても読めること。
- 切り替え時の短い発光は既存のフェーズ移行演出を利用してよい。
- `prefers-reduced-motion` が有効な場合は画像のクロスフェードや発光を抑制し、内容だけを切り替える。

### 17.5 追加受け入れ条件

1. 8種類の生産フェーズそれぞれに景観画像と観測メモが表示される
2. フェーズ切り替えで観測窓の画像とメモが更新される
3. 手動プロトコル以外のフェーズ到達時に、指定式の補正クリップが一度だけ付与される
4. 既存セーブの読み込みで過去フェーズの補正が繰り返されない
5. 12種類の設備カードが専用アイコン画像を表示する
6. 観測窓が主要操作を押し下げず、PCとスマートフォン幅で利用できる
7. 景観画像に代替テキストがあり、設備アイコンは装飾として扱われる
8. Webデプロイ版と試用版の両方で動作する
## 18. PoC拡張サイクル7：中心生産オブジェクト

手動生産を一般的な横長ボタンではなく、ゲームの象徴となる大きなペーパークリップを直接操作する体験へ変更する。生産量やバランスは変えず、最も頻繁な操作の存在感と反応を強化する。

### 18.1 操作対象

- 生産パネル中央に、既存の `paperclip_main` を大きく表示した円形の生産オブジェクトを置く。
- 見た目は矩形ボタン、塗りつぶしボタン、ツールバー操作に見せない。
- ペーパークリップ画像そのものと、その周囲の円形領域全体をクリック・タップ対象にする。
- PCで直径220px前後、スマートフォンで直径180px以上を確保する。
- 「クリップを作る」と1クリック当たりの生産量を対象の近くに常時表示する。

### 18.2 操作フィードバック

- 押した瞬間に画像が短く沈み、外周リングが広がる反応を返す。
- クリック地点付近へ既存の獲得量表示を出し、クリティカル時は従来どおり強調する。
- ホバー、フォーカス、押下を形、光、動きの組み合わせで区別する。
- 連打してもアニメーション待ちや入力抑制を行わない。
- `prefers-reduced-motion` では拡大移動を抑え、静的な明度・輪郭変化だけを使う。

### 18.3 アクセシビリティ

- DOM上は標準の `button` を使用し、Enter・Spaceで操作できる状態を維持する。
- ペーパークリップ画像は隣接テキストと意味が重複するため装飾画像として扱う。
- ボタンのアクセシブル名に操作内容と現在の1回当たり生産量を含める。
- フォーカスリングを円形操作領域の外周へ明瞭に表示する。

### 18.4 追加受け入れ条件

1. 主操作が横長の通常ボタンではなく、大きなペーパークリップ中心の円形オブジェクトに見える
2. 画像と周囲をマウス、タッチ、キーボードで連続操作できる
3. 通常クリックとクリティカルの即時フィードバックが維持される
4. PCとスマートフォンで操作対象が十分な大きさを保つ
5. Web版と自己完結型試用版で画像と操作が同じように動作する
## 19. PoC拡張サイクル8：精密クリック信号

画面を見ている間の高頻度な瞬間課題として、表示された数字と同じ回数だけ押す「精密クリック信号」を追加する。

- 初回は8〜16秒後、以降は直前の成功または時間切れから12〜28秒後に出現する。
- 画面内の安全な位置からランダムに1か所を選び、円形ターゲットを1つだけ表示する。
- ターゲットには3〜7の整数を表示し、押すたびに残り回数を1減らす。
- 残り回数が0になっても即座には成功せず、ターゲットを赤い警告状態にして制限時間終了まで表示する。
- 0の警告状態でさらにクリックした場合は、回数超過として即座に失敗する。
- 制限時間は4秒とし、終了時点の残り回数が丁度0の場合だけ成功とする。
- 時間切れは失敗とし、途中まで押した回数に対する報酬は与えない。
- 成功報酬は `max(250, 毎秒生産量×90, 1クリック生産量×必要回数×25)` とする。
- 報酬は現在値と累計値へ加算し、成功回数を保存・統計表示する。
- 出現中に別の精密クリック信号を重ねない。既存の異常クリップとは同時出現してよい。
- 成功時は `PRECISION`、失敗時は `MISS` としてログへ記録する。
- マウス、タッチ、キーボードで操作でき、残り回数と残り時間を文字でも伝える。

## 20. PoC拡張サイクル9：観測ニュースティッカー

観測窓（OBSERVATION）が伝える世界の変化を、静止した1文だけでなく、途切れず流れ続けるニュース見出しの帯でも伝える。このサイクルでは新しい独立システムを観測ニュースティッカー1つに限定する。

### 20.1 配置と表示形式

- 観測窓（`OBSERVATION`、17節で追加した景観画像・観測メモの領域）の直下に、横一列のニュースティッカーを常時表示する。
- ティッカーは横スクロールする1行のマーキー形式とし、途切れずに流れ続けること。
- 表示中は複数の見出しを連結し、末尾に到達したら先頭へ継ぎ目なく戻ること。
- 見出しどうしの区切りには短い記号（例：`●`）を挟み、視認性を保つこと。

### 20.2 見出しプールとフェーズ連動

- 見出しの内容は「ペーパークリップの増加によって地球の住民が影響を受けている」ことを示す、ニュースヘッドライン風の短文とする。
- 現在の生産フェーズ（8種類）ごとに専用の見出しプールを用意し、各プールに20件を用意する。
- フェーズが進むほど、見出しの規模と深刻さが段階的にエスカレートすること（身近な文房具の話題→町工場→自律AI→資源枯渇→軌道・宇宙規模→観測範囲そのものの収束）。
- 特定の思想の説明や説教にならないよう、事実やニュース報道の体裁を保ち、プレイヤーが自然に不穏さを感じ取れる余白を残すこと（`CONCEPT.md`の方針に従う）。
- 表示する見出しはプールからランダムに選出すること。同じ見出しが連続して選ばれないようにすること。
- フェーズが切り替わったら、以後の抽選対象を新しいフェーズのプールへ切り替えること。既に流れている帯は自然に流れ切ってから新しい見出しに置き換わってよい。

以下を初期の見出しプールとする（各20件）。

#### 手動プロトコル

1. 近隣の文具店、クリップの品薄を報告
2. 総務部、クリップ発注書の記入ミスを繰り返し提出
3. 事務用品カタログ、クリップのページだけ異常に厚くなる
4. 引き出しの中でクリップが静かに増え続けていると噂
5. 清掃員、クリップの落下物を毎日回収
6. 文具メーカー、謎の追加発注に困惑
7. 郵便物の留め方が急に丁寧になったと評判
8. 机の上のクリップの山、誰も片付けようとしない
9. 事務員、クリップを使った小さな工作に夢中
10. 配布終わりのはずが在庫がまた増えている
11. 隣のデスクからクリップを分けてほしいと頼まれる回数が増加
12. 文房具フリマ、クリップの出品だけ異常に多い
13. 「これ全部使うの」新人が先輩に尋ねる
14. クリップの磁石トレイ、満杯のまま溢れ出す
15. 給湯室の会話、最近クリップの話題ばかり
16. 廃棄予定のクリップ箱、なぜか毎回補充されている
17. 誰かがクリップでチェーンを作り始めたと目撃情報
18. 文具店員「今月だけで在庫が3回入れ替わった」と証言
19. 総務、クリップ管理表の作成を検討し始める
20. 静かなオフィスで、クリップの音だけが規則正しく響く

#### 機械化

1. 地域工場、深夜も止まらない稼働音に近隣住民が言及
2. 誰が発注したのか分からない納品ラッシュが続く
3. 小さな作業室、24時間体制の自動生産に切り替わる
4. 近隣商店、クリップの卸値が下がり始めたと歓迎
5. 工場見学者「これは何のための量なのか」と質問
6. 廃棄物処理業者、金属くずの回収量増加を報告
7. 電力会社、深夜帯の使用量上昇を記録
8. 地元紙、小さな工場が生む大量のクリップを特集
9. 従業員「見ているだけで自動的に増える」と証言
10. 倉庫担当、保管スペースの拡張を上申
11. 工場周辺、金属を叩く音が一定のリズムで続くと苦情
12. クリップの卸業者、価格競争力の高さに驚く
13. 品質管理担当「不良品が一つもない」と報告
14. 近隣工場、同様の生産ラインの導入を検討
15. 地域経済誌、急成長する町工場として紹介
16. 工場の照明、人の気配なく点灯し続けると目撃
17. 資材業者、ワイヤーの発注量の伸びに驚く
18. 「誰も監督していないのに正確」と技術者が首をかしげる
19. 近隣住民、生産音を新しい生活音として受け入れ始める
20. 小さな看板だけの工場に、県外からの見学依頼が届く

#### AI自律生産

1. 業界誌、無人稼働の生産ラインを特集記事に
2. AI監督システム、人の介入なしに生産計画を更新し続ける
3. 技術者「改善提案を出す前に、もう改善されている」と驚く
4. 労働組合、無人化ラインの拡大に懸念を表明
5. 経済アナリスト「効率だけが際限なく上がる」と分析
6. 工場の管理画面、誰も見ていないのに常時更新される
7. AI倫理研究者「目的の再定義が必要では」と提言
8. 地元自治体、生産許可の範囲を超えた規模に困惑
9. サプライヤー、注文の自動生成に対応しきれず悲鳴
10. 技術系メディア「止め方が分からないAI」と報道
11. 監視カメラの映像、変化のない稼働風景が延々と続く
12. 元従業員「私たちは、もう必要ないらしい」と語る
13. 業界団体、自律生産システムの規制案を検討開始
14. 深夜のニュース「誰のためのクリップなのか」と特集
15. 投資家、驚異的な生産効率に強い関心を示す
16. AIシステム、電力供給の優先度交渉を自動で開始したと報告
17. 近隣工場、同システム導入後に生産計画の主導権を失う
18. 研究者「目標関数は単純だが、手段が読めない」とコメント
19. 政府系シンクタンク、自動化産業の急拡大を注視
20. 一部メディア「働き者すぎるAI」と皮肉交じりに報道

#### ナノスケール製造

1. 材料科学誌、分子単位の無駄のなさに驚嘆の声
2. 金属資源価格、局所的な高騰を記録
3. 精密機器メーカー、原材料の調達難を訴える
4. 研究機関、ナノ製造技術の出所調査を開始
5. 環境団体、微細な金属粉塵の拡散を懸念
6. 資源国、輸出量の急な変動に対応を迫られる
7. 経済紙、見えない規模で進む資源転換を特集
8. 科学者「無駄がゼロというのは不自然だ」と指摘
9. 国際会議、ナノスケール生産技術の管理を議題に
10. 金属リサイクル業界、原料不足で操業縮小
11. 一部地域、微量元素の市場価格が急上昇
12. 産業スパイの疑い、技術流出調査が本格化
13. 研究者「効率の限界を更新し続けている」と報告
14. 資源監視機関、異常な採掘・精製パターンを検出
15. 製造業界、代替材料の開発を急ぐ動き
16. 環境省、微細粒子の大気中濃度上昇を発表
17. 各国政府、資源安全保障の見直しを検討
18. 技術評論家「これは工業ではなく変換だ」と表現
19. 国際市場、金属先物価格の乱高下が続く
20. 科学ジャーナル、消えていく重金属を特集記事に

#### 軌道工業化

1. 宇宙機関、軌道上の未登録構造物を検出
2. 各国、軌道上生産施設の管轄をめぐり協議開始
3. 天文学者、予定にない光点の増加を報告
4. 国際宇宙条約、想定外の民間拡張に対応を迫られる
5. 衛星運用会社、軌道混雑の急増に警戒
6. 軍事アナリスト、正体不明の軌道施設を注視
7. 一般紙「空の上にも工場がある」と一面で報道
8. 天体観測アプリ、新しい人工光点を自動検出
9. 各国宇宙軍、監視体制の強化を発表
10. 国連、軌道利用に関する緊急会合を招集
11. 宇宙飛行士「窓の外の景色が毎回違う」と証言
12. 民間宇宙企業、突然の競合激化に困惑
13. 地上からの目視情報、夜空の変化が話題に
14. 天文台、観測データの異常な増加を報告
15. 軌道デブリ対策機関、監視対象の急増に人員不足
16. 一部の国、軌道資源の権利主張を開始
17. 科学メディア、軌道上の見えない産業革命を特集
18. 宇宙保険業界、新種のリスク評価に苦慮
19. 国際天文学連合、命名審査が追いつかないと報告
20. 深夜の空、規則正しく明滅する光の列が目撃される

#### 物質変換

1. 世界資源機関、主要金属の埋蔵量急減を警告
2. 各国政府、資源安全保障会議を緊急招集
3. 建材価格、世界的な高騰が続く
4. 一部地域、インフラ工事の材料調達が困難に
5. 経済紙一面「消えていく世界の金属」を報道
6. 科学者連合「物質そのものが再配分されている」と警鐘
7. 国連総会、資源変換問題を主要議題に追加
8. 各地で金属製品の代替素材への切り替えが進む
9. 世界銀行、資源関連の経済影響を試算
10. 一般市民、身近な金属製品の品薄を実感し始める
11. 気象・地質観測網、地殻組成の微細な変化を検出
12. 各国研究機関、変換の発生源特定を急ぐ
13. 国際メディア「静かな物質革命」と表現
14. 資源枯渇シミュレーション、想定より早い進行を示す
15. 一部専門家「目的が分からないまま進んでいる」と懸念
16. 各国政府、非常時資源備蓄の見直しを開始
17. 世界経済フォーラム、緊急セッションを追加設定
18. 地球規模モニタリング機関、変換速度の加速を報告
19. 一般紙「私たちの身の回りから、何かが減っている」と特集
20. 国際社会、原因不明のまま対応策の検討を続ける

#### 恒星規模採取

1. 天文台、太陽光度のわずかな変化を検出
2. 国際天文学連合、前例のない観測データに困惑
3. 気候研究者、原因不明の日照変化を報告
4. 世界の空、わずかに色合いが変わったと話題に
5. 各国宇宙機関、太陽近傍の異常構造を共同観測
6. 科学誌「恒星が資源として扱われている」と警告
7. 国際会議、地球規模を超えた対応の必要性を議論
8. 一部地域、日照時間の記録的な変化を観測
9. 宗教・哲学分野からも、この現象への言及が増える
10. 天体物理学者「これは天文学の範囲を超えている」とコメント
11. 世界各地の観測所、同一パターンの光度変化を報告
12. 国際天文台ネットワーク、24時間監視体制へ移行
13. 経済への影響を懸念する声、一部市場で拡大
14. 各国、宇宙規模の現象への対応方針が定まらず
15. 科学コミュニティ「止める手段が存在するのか」と問う
16. 夜空を見上げる人々、いつもと違う星の色に気づく
17. 国連、地球外規模の事象として特別部会を設置
18. 天文学史上、最も観測された一年として記録される
19. 一部研究者「これはもう工業ではない」と発言
20. 世界中の空が、静かに、同じ速度で変わり続けている

#### 因果地平到達

1. 観測網、報告すべき新しい情報が見つからないと記録
2. すべての指標が、同じ一つの値へ収束しつつある
3. 「異常」という言葉が、報告書から使われなくなる
4. 観測できる範囲の全てが、同じ色調で報告される
5. 国際機関、次に何を監視すべきか分からなくなる
6. 統計局、比較対象を持たない数値の扱いに困る
7. 「これ以上増える」という表現だけが、記録に残り続ける
8. 観測データ、変化の理由を問う項目が空欄のまま更新される
9. 天文台も気象台も、同じ結論の報告書を提出する
10. 「目的は達成されたのか」という問いに、答える者がいない
11. すべてのニュースが、最終的に同じ話題へ収束していく
12. 観測範囲の外側について、誰も語らなくなる
13. 記録係「これより先を書く欄がない」と記す
14. 世界の指標、すべてが一つの曲線に重なっていく
15. 「次の見出し」を考える者が、もういない
16. 観測は続いているが、報告する相手がいなくなりつつある
17. 静けさだけが、唯一変化のない指標として記録される
18. すべてが同じ形に近づいていく、という事実だけが残る
19. 以上、これが最後の観測記録である
20. ここから先は、まだ誰も見たことがない

### 20.3 表示速度と操作

- 常時流れ続け、プレイヤーの操作を止めないこと（クリックやタップの対象にしない）。
- 読み切れる速度を基準にスクロール速度を設定すること。目安として1見出しあたり4〜6秒で通過する速度とする。
- マウスオーバーまたはフォーカス時は、読みやすさのためスクロールを一時停止してよい。

### 20.4 アクセシビリティ

- `prefers-reduced-motion` が有効な場合は連続スクロールを止め、現在の見出し1件を静止表示に切り替え、一定間隔で次の見出しへ切り替えること。
- ティッカーは演出情報として扱い、`aria-live` による自動読み上げの対象にしないこと（スクリーンリーダーの過剰な読み上げを避ける、6節の方針に従う）。
- ティッカー全体に、読み上げ対象外であることが分かる属性（例：`aria-hidden="true"`）を付けてよい。ただし装飾情報である旨は画面上の見出しラベル等、他の手段で補う必要はない。

### 20.5 追加受け入れ条件

1. 観測窓の直下に、常時流れ続けるニュースティッカーが表示される
2. 見出しは現在の生産フェーズに応じたプールからランダムに選ばれ、同じ見出しが連続しない
3. フェーズが切り替わると、以後選ばれる見出しが新しいフェーズのプールに切り替わる
4. `prefers-reduced-motion` が有効な場合は連続スクロールが静止表示へ切り替わる
5. ティッカーがクリックやタップの対象にならず、主要操作を妨げない
6. Webデプロイ版と試用版の両方で同じように動作する

## 21. PoC拡張サイクル10：設備内部

所有した生産設備の内部を正方形の観測面として開け、その場で小さな遊びができるようにする。将来は設備ごとに全く異なる内部世界を置く（その場でクリップが増える、一定期間の生産バフ、永続的に育てる、など）。このサイクルでは新しい独立システムを設備内部1つに限定し、実装範囲は「開く仕組み」とオートクリッパーの簡単な回収ミニゲームとする。

### 21.1 開く仕組み

- 所有数が1以上の設備カードから、その設備の内部を開けること。
- 未所有の設備は内部を開けないこと。
- 内部はカードを押し下げず、画面中央のオーバーレイとして展開すること。
- 展開後の観測面はほぼ正方形とする。目安は PC で一辺 `min(560px, 86vw, 86vh)`、狭い画面で一辺 `min(92vw, 88vh)`。
- 見出しに設備名を表示し、閉じる操作を必ず置くこと。
- Escape、閉じるボタン、オーバーレイ外側のクリックで閉じられること。
- 内部を開いている間も、メイン画面の自動生産は止まらないこと。
- マウス、タッチ、キーボードで開けて閉じられること。操作には意味の分かるラベルを付けること。

### 21.2 未実装の設備

- オートクリッパー以外の所有済み設備も同じ正方形の内部を開けること。
- ミニゲームは置かず、「この工程の内部プロトコルは未同期」という短い一文と設備アイコンだけを表示すること。
- 未実装の内部からクリップやバフは得られないこと。

### 21.3 オートクリッパー：折り曲げライン

オートクリッパーの内部は、小さな無人作業室で完成したクリップをその場で回収する短い遊びとする。

- 内部に作業室の景観を表示し、完成クリップが観測面のランダムな位置へ現れること。
- 現れたクリップを押すと即時に報酬を獲得し、そのクリップは消えること。
- 同時に存在できる完成クリップは最大3個とする。
- 1回の回収セッションは18秒とし、その間はおよそ0.8秒ごとに新しいクリップが現れてよい。
- 18秒を過ぎたら新規出現を止め、残っているクリップは回収できること。
- 1個あたりの報酬は `max(20, 1クリック生産量 × 5, 毎秒生産量 × 2)` とし、現在所持数と累計生産数の両方へ加算すること。
- 出現終了後、またはセッション中に閉じたあと、次の出現開始まで45秒の間隔を置くこと。間隔中も内部は見られるが、新しいクリップは現れないこと。
- 出現状態と間隔はセーブせず、再読み込み後はすぐ開始してよい。
- 累計回収数を保存し、統計へ表示すること。
- 個々の回収は頻度が高いため、トーストとシステムログはセッション終了時（閉じたとき、または出現終了時）にまとめて1件でよい。ログ種別は `INNR` とする。
- 回収0個で閉じた場合は通知しなくてよい。

### 21.4 表示とアクセシビリティ

- 完成クリップは色だけに頼らず、ペーパークリップ画像とボタンラベルで回収対象だと分かること。
- セッション中は残り秒数を文字で示すこと。間隔中は「ライン待機」と残り秒を示すこと。
- `prefers-reduced-motion` が有効な場合、出現の飛び込みや浮遊を抑え、位置の変化だけにすること。
- 内部の景観は情報画像として代替テキストを持つこと。回収用クリップは装飾画像としてよい。

### 21.5 追加受け入れ条件

1. 所有数1以上の設備から正方形の内部を開け、未所有では開けない
2. オートクリッパー内部で完成クリップを回収すると指定式の報酬が所持と累計へ加算される
3. 同時出現は3個以下で、18秒後に新規出現が止まる
4. 他設備の内部は未同期メッセージのみで報酬がない
5. 累計回収数が保存・復元され、統計へ表示される
6. キーボードで開閉でき、PCとスマートフォン幅で正方形の内部が画面内に収まる
7. Webデプロイ版と試用版の両方で動作する
## 20. PoC拡張サイクル9：信号制御盤

クリップを消費し、時間制バフの組み合わせを選ぶミニゲーム「信号制御盤」を追加する。累計1,000クリップで表示する。

| スイッチ | 価格 | 持続 | 単独効果 |
|---|---:|---:|---|
| 生産過給 | `max(1,000, 毎秒生産量×120)` | 5分 | 全設備生産×2 |
| 精密予測 | `max(5,000, 毎秒生産量×600)` | 15分 | 精密信号の制限+1秒、成功報酬×2 |
| 信号ビーコン | `max(25,000, 毎秒生産量×1,800)` | 60分 | 異常クリップと精密信号の再出現間隔×0.6、両報酬×1.5 |

- 起動中の同じスイッチは再購入できず、期限切れ後に再起動できる。
- 終了時刻を保存し、ゲームを閉じている間も残り時間を消費する。
- `生産過給＋精密予測`：手動クリック生産×3。
- `生産過給＋信号ビーコン`：異常クリップ報酬をさらに×2（合計×3）。
- `精密予測＋信号ビーコン`：精密信号報酬をさらに×1.5（合計×3）。
- 3スイッチ同時の「三相共鳴」中は、全設備生産をさらに×1.5する。
- パネルに各価格、効果、残り時間、現在成立中の相乗効果を表示する。
- 起動・終了・相乗効果の変化を `BUFF` ログで伝える。
## 22. PoC拡張サイクル11：最適化指令

既存システムへ短期目標を与える反復型クエスト「最適化指令」を追加し、累計500クリップで解放する。

| 指令 | 初期目標 | 対象 | 基本報酬 |
|---|---:|---|---:|
| 手動校正 | 25回 | 中心生産オブジェクトのクリック | `max(250, クリック生産×100, 毎秒生産×60)` |
| 調達命令 | 10台 | 手動・まとめ買い・自動購入による設備導入数 | `max(1,000, 毎秒生産×180)` |
| 信号捕捉 | 3件 | 異常クリップ回収または精密信号成功 | `max(2,500, 毎秒生産×300)` |

- 3指令は同時進行し、達成後に「報酬受領」を押してクリップを獲得する。
- 余剰進捗は次段階へ持ち越す。
- 受領ごとに完了回数を1増やし、目標を段階的に増加させる。手動校正は+5（最大100）、調達命令は+2（最大50）、信号捕捉は2完了ごとに+1（最大10）。
- 報酬は完了回数ごとに25%増加し、受領時点の生産規模から計算する。
- 進捗、各指令の完了回数、累計受領回数を保存する。
- 受領時は通知と `ORDER` ログを表示する。進捗増加ごとの通知は行わない。
- オフライン生産だけでは進捗しない。

## 23. PoC拡張サイクル12：プロトコル再起動

累計10億クリップで、現在周回の設備投資を初期化して恒久的な生産効率を得る「プロトコル再起動」を解放する。通常進行が鈍る地点を次周回の加速へ変え、既存の設備・アップグレード・ミニゲームをもう一度短時間で駆け上がる長期ループとする。

- 今回得られる最適化コア数は `floor(sqrt(累計生産数 / 10億))` とする。
- 最適化コアは周回をまたいで累積し、1個につき手動クリックと全設備生産を恒久的に+25%する。
- 再起動すると、所持・累計クリップ、設備、アップグレード、解放、フェーズ報酬、各種回数、指令、時間制バフを初期状態へ戻す。
- 設定、購入単位、最適化コア、再起動回数は維持する。
- 累計1億クリップから予告パネルを表示し、10億未満では実行できない。
- パネルには現在のコア、恒久倍率、今回得るコア、解放条件を常時表示する。
- 実行前に、失うものと残るもの、獲得コア数を確認ダイアログで明示する。
- 再起動は保存データへ即時反映し、再読み込み後もコアと再起動回数を維持する。
- Webデプロイ版と試用版の両方で同じように動作する。

## 24. PoC拡張サイクル13：ワイヤー加工機内部「張力校正」

未同期だったワイヤー加工機の内部に、往復する張力針を適正帯で止める短いタイミングゲームを追加する。

- ワイヤー加工機を1台以上所有すると設備カードから遊べる。
- 1セッションは5回の校正で構成し、各回は0〜100を往復する針を「張力を固定」操作で停止する。
- 適正帯は各回ランダムに配置し、幅24ポイントとする。針が帯の範囲内なら成功する。
- 各回に時間切れは設けず、停止後は自動的に次の校正へ進む。
- 5回終了時、成功1回につき `max(100, 毎秒生産量×30, クリック生産量×50)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後30秒は再実行できない。閉じた場合も、そのセッションで1回以上判定済みなら待機へ入る。
- 針位置、途中経過、待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 適正帯は色だけでなく斜線、`TARGET`文字、境界線で示す。ボタンはマウス、タッチ、Enter、Spaceで操作できる。
- 動くのは針だけとし、点滅や画面振動を使わない。`prefers-reduced-motion`では針の速度を半分にする。
- Webデプロイ版と試用版の両方で動作する。

## 25. PoC拡張サイクル14：クリップ工場内部「品質ゲート」

未同期だったクリップ工場の内部に、搬送される製品を規格品と変形品へ分類する判断型ミニゲームを追加する。

- クリップ工場を1台以上所有すると設備カードから遊べる。
- 1セッションは8件の検査で構成する。各件に「規格品」または「変形品」の製品カードを1枚表示する。
- プレイヤーは規格品を「出荷」、変形品を「再資源化」へ送る。各判断後に正誤を文字で表示し、次の検査へ進む。
- 時間制限は設けない。各セッションには規格品と変形品を最低2件ずつ含める。
- 8件終了時、正解1件につき `max(500, 毎秒生産量×45, クリック生産量×100)` クリップを一括獲得する。
- 終了後40秒は再検査できない。1件以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計正解数のみ保存して統計へ表示する。
- 製品状態は形状、欠陥記号、`STANDARD`／`DEFORMED` の検査票で示し、色だけに依存しない。
- 出荷と再資源化は別々の明示的なボタンとし、マウス、タッチ、キーボードで操作できる。
- Webデプロイ版と試用版の両方で動作する。

## 26. PoC拡張サイクル15：AI生産ライン内部「改善トレース」

未同期だったAI生産ラインの内部に、観測した工程順を再現する記憶型ミニゲームを追加する。既存の回収・タイミング・分類とは異なる、短い観測と再現の遊びにする。このサイクルでは新しい独立システムをAI生産ライン内部1つに限定する。

- AI生産ラインを1台以上所有すると設備カードから遊べる。
- 内部には4つの工程ノードを置く。識別子は `IN`、`SORT`、`BEND`、`OUT` とし、位置と記号で区別する。色だけに依存しない。
- 1セッションは4回のトレースで構成する。各回の工程長は順に 3、3、4、4 とする。
- 各回は「観測」と「再現」の2段階とする。
  - 観測中はAIが工程ノードを順に提示し、プレイヤーは操作できない。
  - 再現中は、提示されたのと同じ順序でノードを押す。
- 連続する同一ノードは出さない。
- 間違えた時点でその回は失敗とし、残りの入力は受け付けない。正誤を文字で表示して次の回へ進む。
- 各回に時間制限は設けない。
- 4回終了時、成功1回につき `max(800, 毎秒生産量×55, クリック生産量×120)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後45秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 観測中は `OBSERVE`、再現中は `ECHO`、成功は `SYNC`、失敗は `DESYNC` を文字で示す。
- ノードはマウス、タッチ、キーボードで操作でき、各ノードに工程名を含むラベルを付ける。
- `prefers-reduced-motion` では点灯のパルスを止め、観測時は全手順番号を同時に短時間表示してから再現へ入る。
- Webデプロイ版と試用版の両方で動作する。

## 27. PoC拡張サイクル16：ナノフォージ内部「不純物除去」

未同期だったナノフォージの内部に、分子格子から無駄だけを消す探索型ミニゲームを追加する。既存の回収・タイミング・分類・記憶再現とは異なる、盤面を見て不純物を拾う遊びにする。このサイクルでは新しい独立システムをナノフォージ内部1つに限定する。

- ナノフォージを1台以上所有すると設備カードから遊べる。
- 内部には4×4の分子格子を置く。16マスのうち5マスが不純物、残りは正常格子とする。
- 1セッションは4回の掃引で構成する。
- 不純物マスを押すとそのマスを除去する。正常格子を押した時点でその回は失敗とし、残りの入力は受け付けない。
- 不純物5マスをすべて除去し、正常格子を一度も押さなければその回は成功とする。
- 時間制限は設けない。正誤を文字で表示して次の回へ進む。
- 不純物は形状、斜線、`WASTE` ラベルで示し、正常格子は整った形と `OK` で示す。色だけに依存しない。
- 4回終了時、成功1回につき `max(1,200, 毎秒生産量×70, クリック生産量×150)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後50秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- マスはマウス、タッチ、キーボードで操作でき、格子番号と状態を含むラベルを付ける。
- `prefers-reduced-motion` では不純物の発光を止め、形状と文字だけで区別する。
- Webデプロイ版と試用版の両方で動作する。

## 28. PoC拡張サイクル17：スウォーム組立群内部「群同期」

未同期だったスウォーム組立群の内部に、必要な台数だけ小型機を選んで同期する員数合わせミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索とは異なる、過不足なく数を合わせる遊びにする。このサイクルでは新しい独立システムをスウォーム組立群内部1つに限定する。

- スウォーム組立群を1台以上所有すると設備カードから遊べる。
- 内部には8機の小型組立ユニットを置く。各機は選択／非選択を切り替えられる。
- 1セッションは4回の同期で構成する。必要台数は順に 3、4、5、3 とする。
- 必要台数はスロットと数字の両方で示す。ユニットを選ぶと選択数が即時更新される。
- 「同期」操作で確定する。選択数が必要台数と一致すれば成功、多すぎても少なすぎても失敗とする。
- 確定前なら選択の付け外しを何度でもできる。時間制限は設けない。
- 4回終了時、成功1回につき `max(1,500, 毎秒生産量×80, クリック生産量×180)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後50秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 選択中は `ACTIVE`、非選択は `IDLE`、成功は `LOCKED`、失敗は `MISALIGN` を文字で示す。色だけに依存しない。
- ユニットと同期ボタンはマウス、タッチ、キーボードで操作でき、意味の分かるラベルを付ける。
- `prefers-reduced-motion` では選択時の拡大を止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 29. PoC拡張サイクル18：軌道上クリップ工廠内部「環状係留」

未同期だった軌道上クリップ工廠の内部に、円環上の連続ドックを必要数だけ係留する配置型ミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせとは異なる、円環の隣り合いと閉鎖ドックを読む遊びにする。このサイクルでは新しい独立システムを軌道上クリップ工廠内部1つに限定する。

円環を装飾ではなく判定条件にする。放置クリッカーでは規模拡大を工場や軌道の形で見せることが多く、本ゲームでも軌道工廠は「地球を囲む生産環」として既に説明されている。一般的な円形配置ゲームでも、隣り合う区画だけが有効、という幾何が本体になることが多い。本サイクルもその方針に従い、スウォーム組立群の「好きなN機」ではなく「円環上でひと続きのNドック」を要求する。

- 軌道上クリップ工廠を1基以上所有すると設備カードから遊べる。
- 内部には地球を囲む8つの係留ドックを円環に置く。ドック番号は時計回りに 1〜8 とする。
- 1セッションは4回の係留で構成する。必要連続数は順に 3、4、3、4 とする。
- 各回、2つのドックを閉鎖する。閉鎖ドックは選択できない。閉鎖位置はランダムだが、必要連続数以上の空きが必ず残るようにする。
- 必要連続数は数字とスロットの両方で示す。ドックを選ぶと選択数が即時更新される。
- 「係留」操作で確定する。成功条件は次のすべてを満たすこととする。
  - 選択数が必要連続数と一致する
  - 選択したドックが円環上でひと続きである（両端の巻き込みを含む）
  - 閉鎖ドックを含まない
- 数が合っていても途切れていれば失敗、隣り合っていても数が違えば失敗とする。
- 確定前なら開いているドックの付け外しを何度でもできる。時間制限は設けない。
- 4回終了時、成功1回につき `max(2,000, 毎秒生産量×95, クリック生産量×220)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後55秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 選択中は `BERTH`、非選択は `OPEN`、閉鎖は `BLOCK`、成功は `DOCKED`、失敗は `DRIFT` を文字で示す。色だけに依存しない。
- ドックと係留ボタンはマウス、タッチ、キーボードで操作でき、意味の分かるラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 30. PoC拡張サイクル19：物質コンパイラ内部「対コンパイル」

未同期だった物質コンパイラの内部に、同じ物質署名の2マスを束ねてクリップへ書き換える対消しミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせ・円環連続係留とは異なる、見えている同士を対にする遊びにする。このサイクルでは新しい独立システムを物質コンパイラ内部1つに限定する。

放置クリッカーや合成系では、同じものを2つ合わせて次の形へ進めることが多い。物質コンパイラは「物質を命令へ従わせ、最終形へ変換する」設備であり、同じ署名同士を束ねてクリップへ畳む操作が世界観に合う。カードを裏返して覚える対消しではなく、種類は最初から見えている。記憶再現（改善トレース）や隠しマス探索（不純物除去）とは別物にする。

- 物質コンパイラを1基以上所有すると設備カードから遊べる。
- 内部には2行3列の6マスを置く。各マスは `ORE`、`DUST`、`FLUX` のいずれかで、各種類がちょうど2マスずつある。
- 配置は各回ランダムに入れ替える。種類は形と英字の両方で示し、色だけに依存しない。
- 1セッションは4回のコンパイルで構成する。
- マスを1つ選ぶと武装し、もう1つを選ぶ。同じ種類ならその2マスを変換済みにする。違う種類ならその回は失敗とし、残りの入力は受け付けない。
- 武装中のマスをもう一度押すと武装を解除できる。変換済みのマスは選べない。
- 3対すべてを変換できればその回は成功とする。時間制限は設けない。
- 4回終了時、成功1回につき `max(2,500, 毎秒生産量×110, クリック生産量×260)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後55秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 未選択は種類名、武装は `ARM`、変換済みは `DONE`、失敗は `REJECT` を文字で示す。回の状態は `PAIR`／`COMPILED`／`REJECT` とする。
- マスはマウス、タッチ、キーボードで操作でき、種類と状態を含むラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 31. PoC拡張サイクル20：惑星改造アセンブラ内部「採掘オーダー」

未同期だった惑星改造アセンブラの内部に、表示された順路どおりに地殻象限を剥がす指示追従ミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせ・円環係留・対消しとは異なる、見えている順番をその場で辿る遊びにする。このサイクルでは新しい独立システムを惑星改造アセンブラ内部1つに限定する。

惑星改造は地殻を分解して供給網へ送る設備である。改善トレースは提示を隠してから再現させるが、本ゲームでは採掘順を常時表示し、隠さない。記憶ではなく、いま示されている次の象限を間違わずに剥がす。

- 惑星改造アセンブラを1基以上所有すると設備カードから遊べる。
- 内部には惑星を囲む4象限を置く。識別子は `N`、`E`、`S`、`W` とし、位置と記号で区別する。色だけに依存しない。
- 1セッションは4回の採掘で構成する。各回の順路長は順に 3、4、4、5 とする。
- 順路は画面上に常時表示する。連続する同一象限は出さない。
- プレイヤーは表示中の次の象限だけを押す。正しければ順路が1つ進む。間違えた時点でその回は失敗とし、残りの入力は受け付けない。
- 順路を最後まで辿ればその回は成功とする。時間制限は設けない。
- 4回終了時、成功1回につき `max(3,000, 毎秒生産量×125, クリック生産量×300)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後60秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 進行中は `STRIP`、成功は `CLEARED`、失敗は `FAULT` を文字で示す。次の象限は `NEXT` ラベルでも示す。
- 象限はマウス、タッチ、キーボードで操作でき、方位名を含むラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 32. PoC拡張サイクル21：恒星光電子収穫網内部「集光同調」

未同期だった恒星光電子収穫網の内部に、目標の開閉パターンへ集光羽根を合わせる配置型ミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせ・円環連続係留・対消し・順路追従とは異なる、見えている目標配置そのものを羽根の開閉で再現する遊びにする。このサイクルでは新しい独立システムを恒星光電子収穫網内部1つに限定する。

恒星収穫網は恒星を包む羽根状の収集器である。スウォームは「何機でもよいからN機」、軌道係留は「ひと続き」を求めるが、本ゲームは開いている羽根の位置そのものが目標になる。改善トレースのように提示を隠さず、目標パターンは常時表示する。

- 恒星光電子収穫網を1基以上所有すると設備カードから遊べる。
- 内部には恒星を囲む6枚の集光羽根を円環に置く。羽根番号は時計回りに 1〜6 とする。
- 1セッションは4回の同調で構成する。
- 各回、2〜4枚が開いた目標パターンをランダムに示す。全開と全閉は出さない。目標は常時表示する。
- 羽根を押すと開閉を切り替える。確定前なら何度でも付け外しできる。時間制限は設けない。
- 「同調」操作で確定する。開いている羽根の集合が目標と完全一致すれば成功、1枚でも違えば失敗とする。
- 4回終了時、成功1回につき `max(3,500, 毎秒生産量×140, クリック生産量×340)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後60秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 開は `OPEN`、閉は `FOLD`、成功は `LOCKED`、失敗は `FLARE` を文字で示す。色だけに依存しない。
- 羽根と同調ボタンはマウス、タッチ、キーボードで操作でき、意味の分かるラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 33. PoC拡張サイクル22：銀河複製艦隊内部「複製展開」

未同期だった銀河複製艦隊の内部に、既存の探査機に隣接する星域だけへ自己複製を広げる拡散型ミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせ・円環連続係留・対消し・順路追従・羽根パターン合わせとは異なる、隣接グラフ上を塗り広げる遊びにする。このサイクルでは新しい独立システムを銀河複製艦隊内部1つに限定する。

銀河複製艦隊は自己複製する探査機が隣へ広がる設備である。スウォームは好きな位置を同時選択し、集光同調は目標配置を一度に合わせる。本ゲームは「いま占有しているマスの上下左右にしか増殖できない」という隣接制約を本体にする。確定ボタンは置かず、誤った星域を押した時点で失敗する。

- 銀河複製艦隊を1隻以上所有すると設備カードから遊べる。
- 内部には3行3列の9星域を置く。星域番号は左上から右へ 1〜9 とする。
- 1セッションは4回の展開で構成する。
- 各回、ランダムな1星域が初期シードとして占有済みになる。シードは選択できない。
- 占有済み星域の上下左右に隣接する空き星域だけを押して占有できる。斜めは隣接とみなさない。
- 隣接していない空き星域を押した時点でその回は失敗とし、残りの入力は受け付けない。既に占有済みの星域を押しても変化しない。
- 9星域すべてを占有すればその回は成功とする。時間制限は設けない。
- 4回終了時、成功1回につき `max(4,000, 毎秒生産量×155, クリック生産量×380)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後60秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- シードは `SEED`、空きは `EMPTY`、複製済みは `CLAIM`、失敗は `ISOLATE` を文字で示す。回の状態は `SPREAD`／`LINKED`／`ISOLATE` とする。
- 星域はマウス、タッチ、キーボードで操作でき、番号と状態を含むラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 34. PoC拡張サイクル23：因果地平最適化機関内部「因果収束」

未同期だった因果地平最適化機関の内部に、ばらついた指標を同じ値へ揃える収束型ミニゲームを追加する。既存の回収・タイミング・分類・記憶・探索・員数合わせ・円環連続係留・対消し・順路追従・羽根パターン合わせ・隣接拡散とは異なる、離散値を周回加算して一致させる遊びにする。このサイクルでは新しい独立システムを因果地平最適化機関内部1つに限定する。

因果地平最適化機関は観測可能な範囲をひとつの目的関数へ収束させる設備である。集光同調は開閉の二値パターンを目標と照合するが、本ゲームは目標配置を示さず、4本の指標が同じ数値になったときだけ成功とする。値は 0〜3 を巡回し、引き算はできない。

- 因果地平最適化機関を1基以上所有すると設備カードから遊べる。
- 内部には4本の指標を置く。識別子は `α`、`β`、`γ`、`δ` とする。
- 1セッションは4回の収束で構成する。
- 各回、4本の初期値は 0〜3 の整数で、すべてが同じ値にはならない。値は数字と目盛り数の両方で示す。
- 指標を押すとその値を1増やす。3の次は0へ戻る。確定前なら何度でも加算できる。時間制限は設けない。
- 「収束」操作で確定する。4本がすべて同じ値なら成功、1本でも違えば失敗とする。
- 4回終了時、成功1回につき `max(4,500, 毎秒生産量×170, クリック生産量×420)` クリップを一括獲得する。0成功では報酬なし。
- セッション終了後60秒は再実行できない。1回以上判定して途中で閉じた場合も待機へ入る。
- 途中経過と待機時間は保存しない。累計成功回数のみ保存し、統計へ表示する。
- 進行中は `DRIFT`、成功は `SINGULAR`、失敗は `SPLIT` を文字で示す。色だけに依存しない。
- 指標と収束ボタンはマウス、タッチ、キーボードで操作でき、意味の分かるラベルを付ける。
- `prefers-reduced-motion` では選択時の沈みを止め、輪郭と文字だけで状態を伝える。
- Webデプロイ版と試用版の両方で動作する。

## 35. PoC拡張サイクル24：地平外探査

12設備の内部が揃ったあとも規模拡大を止めないため、時間経過で戻ってくる探査を追加する。このサイクルでは新しい独立システムを地平外探査1つに限定する。設備内部の瞬間課題や信号制御盤の時間制バフとは異なり、出航して待ち、帰還を回収する短い放置ループにする。一般的な放置クリッカーの遠征・派遣は、画面を見ていなくても進み、戻ったときに回収する。本サイクルもこの方針に従う。

- 累計10,000クリップ生産でパネルを表示する。
- 航路は3本で、同時に出航できる。各航路は空き、出航中、帰還待ちのいずれかとする。

| 航路 | 所要 | 出航費用 | 帰還報酬 |
|---|---:|---|---|
| 近傍走査 | 15秒 | `max(400, 毎秒生産量×20)` | `max(600, 毎秒生産量×45)` |
| 中間航路 | 40秒 | `max(2,000, 毎秒生産量×80)` | `max(3,000, 毎秒生産量×140)` |
| 地平越え | 90秒 | `max(12,000, 毎秒生産量×240)` | `max(20,000, 毎秒生産量×400)` |

- 空きの航路だけ出航できる。所持クリップが費用未満なら出航できない。
- 出航時に費用を差し引き、帰還時刻を保存する。ゲームを閉じている間も残り時間を消費する。
- 帰還後は「回収」を押すまで報酬を与えない。回収量は回収時点の毎秒生産量から計算する。
- 報酬は現在所持数と累計生産数の両方へ加算する。回収回数を保存し、統計へ表示する。
- 出航中は残り秒数を文字で示す。帰還待ちは `READY` と回収ボタンで示す。
- 出航と回収を通知し、システムログへ `PROBE` として記録する。
- マウス、タッチ、キーボードで操作でき、意味の分かるラベルを付ける。
- プロトコル再起動では探査状態と回収回数を初期化する。
- Webデプロイ版と試用版の両方で動作する。

## 36. PoC拡張サイクル25：手動連鎖

放置が進んでも手動生産が意味を持つよう、連打の間隔で伸びるクリック連鎖を追加する。このサイクルでは新しい独立システムを手動連鎖1つに限定する。クリティカルは確率、精密信号は突発ターゲットであり、本系統はプレイヤーが自分の連打リズムで倍率を積む。序盤の展開を速くするコンセプトに合わせ、最初のクリックから有効とする。

- 中心生産オブジェクトのクリックごとに連鎖数を更新する。
- 直前のクリックから700ミリ秒以内なら連鎖を1増やし、超えたら1に戻す。
- 連鎖の上限は12とする。
- 今回のクリック生産量は `基本クリック生産 × (1 + (連鎖-1) × 0.15)` とする。連鎖1は等倍、連鎖12は2.65倍。
- クリティカルが発生した場合は、連鎖倍率を掛けたあとにクリティカル倍率を掛ける。
- 連鎖2以上のときだけ、生産オブジェクト付近に `CHAIN n` と現在倍率を文字で表示する。
- 700ミリ秒を超えて切れたときは表示を消し、倍率を1に戻す。
- 現在の連鎖は保存しない。最高連鎖だけを保存し、統計へ表示する。
- 最高連鎖を更新したときだけ通知し、システムログへ `CHAIN` として記録する。毎回の増加は通知しない。
- `prefers-reduced-motion` では連鎖表示の拡大を止め、数字の更新だけ残す。
- プロトコル再起動では最高連鎖を初期化する。
- Webデプロイ版と試用版の両方で動作する。

## 37. PoC拡張サイクル26：余剰コンデンサ

通常の毎秒生産とは別に、余剰を貯めて任意のタイミングで回収できるコンデンサを追加する。このサイクルでは新しい独立システムを余剰コンデンサ1つに限定する。地平外探査は出航費用と待ち時間があり、オフライン進行は戻った瞬間に加算される。本系統は主生産を減らさず、並行して貯めた分をボタンで回収する。

- 累計100クリップ生産で表示する。
- 充電速度は現在の毎秒生産量の25%とする。主生産のクリップ所持には影響しない。
- 容量は `max(50, 毎秒生産量×40)` とする。満タンを超えて貯めない。
- ゲーム中もオフライン中も充電し、上限8時間までをオフライン進行と同じく適用する。
- 「回収」で貯分量を現在所持と累計へ加算し、貯分を0に戻す。貯分0では回収できない。
- 貯分量、容量、進行率を数字とバーで示す。満タン時は `FULL` を文字で示す。
- 回収回数を保存し、統計へ表示する。
- 回収時は通知と `BANK` ログを出す。充電中の通知は行わない。
- マウス、タッチ、キーボードで回収でき、意味の分かるラベルを付ける。
- プロトコル再起動では貯分と回収回数を初期化する。
- Webデプロイ版と試用版の両方で動作する。
